--- origsrc/Celero-2.8.2/CMakeLists.txt	2021-03-26 09:34:48.000000000 +0900
+++ src/Celero-2.8.2/CMakeLists.txt	2021-07-21 15:58:22.767395000 +0900
@@ -206,6 +206,10 @@ target_include_directories(${PROJECT_NAM
   $<INSTALL_INTERFACE:${CMAKE_INSTALL_PREFIX}/include>
 )
 
+if (CYGWIN)
+	set_target_properties(${PROJECT_NAME} PROPERTIES SOVERSION 0)
+endif()
+
 # ---------------------------------------------------------------------------
 # Install and exports
 # ---------------------------------------------------------------------------
@@ -220,9 +224,15 @@ install(TARGETS ${PROJECT_NAME}
 install(DIRECTORY include DESTINATION ${CMAKE_INSTALL_PREFIX})
 
 # export to be used from install location
-install(EXPORT ${PROJECT_NAME}-target
-  DESTINATION ${CMAKE_INSTALL_PREFIX}/share
-)
+if (CYGWIN)
+	install(EXPORT ${PROJECT_NAME}-target
+		DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/cmake/Celero
+	)
+else()
+	install(EXPORT ${PROJECT_NAME}-target
+		DESTINATION ${CMAKE_INSTALL_PREFIX}/share
+	)
+endif()
 
 # export to be used from build directory
 export(EXPORT ${PROJECT_NAME}-target
@@ -325,3 +335,10 @@ if(CELERODEVOPS_ENABLE_TSAN)
     set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fuse-ld=gold")
     set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fsanitize=thread")
 endif()
+
+SET(prefix ${CMAKE_INSTALL_PREFIX})
+SET(exec_prefix ${CMAKE_INSTALL_PREFIX})
+SET(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+SET(includedir ${CMAKE_INSTALL_PREFIX}/include)
+CONFIGURE_FILE(${CMAKE_SOURCE_DIR}/Celero.pc.in ${CMAKE_BINARY_DIR}/Celero.pc @ONLY)
+INSTALL(FILES ${CMAKE_BINARY_DIR}/Celero.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/Celero-2.8.2/Celero.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/Celero-2.8.2/Celero.pc.in	2021-07-21 16:01:15.301497200 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: Celero
+Description: C++ Benchmark Authoring Library/Framework
+Version: @VERSION@
+Libs: -L${libdir} -lcelero
+Cflags: -I${includedir}
