--- origsrc/OpenJPH-0.10.3/tests/CMakeLists.txt	2024-01-09 09:15:19.000000000 +0900
+++ src/OpenJPH-0.10.3/tests/CMakeLists.txt	2024-01-11 15:24:26.694583900 +0900
@@ -1,6 +1,9 @@
+include(FetchContent)
+
+find_Package(GTest)
+if (NOT GTest_FOUND)
 
 # Fetch googletest
-include(FetchContent)
 FetchContent_Declare(
   googletest
   URL https://github.com/google/googletest/archive/refs/tags/v1.13.0.tar.gz
@@ -10,6 +13,8 @@ set(gtest_force_shared_crt ON CACHE BOOL
 set(BUILD_GMOCK OFF CACHE BOOL "" FORCE)
 FetchContent_MakeAvailable(googletest)
 
+endif()
+
 # Fetch test files
 FetchContent_Declare(
   jp2k_test_codestreams
@@ -39,8 +44,12 @@ target_link_libraries(
   GTest::gtest_main
 )
 
+if (GTest_FOUND)
+add_test(Tests test_executables)
+else()
 include(GoogleTest)
 gtest_add_tests(TARGET test_executables)
+endif()
 
 if (MSVC)
   if(CMAKE_BUILD_TYPE MATCHES "Debug")
