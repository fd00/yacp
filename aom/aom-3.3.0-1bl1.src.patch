--- origsrc/aom/CMakeLists.txt	2022-04-26 12:16:46.000000000 +0900
+++ src/aom/CMakeLists.txt	2022-04-26 13:27:03.606017900 +0900
@@ -739,7 +739,7 @@ if(ENABLE_EXAMPLES AND "${CMAKE_GENERATO
 endif()
 
 if(BUILD_SHARED_LIBS)
-  if(NOT WIN32 AND NOT APPLE)
+  if(NOT WIN32 AND NOT APPLE AND NOT CYGWIN)
     # The -z defs linker option reports unresolved symbol references from object
     # files when building a shared library.
     if("${CMAKE_VERSION}" VERSION_LESS "3.13")
