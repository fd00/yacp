HOMEPAGE="https://github.com/sevilaybayatli/${PN}"
GIT_URI="git://github.com/sevilaybayatli/${PN}.git"

CATEGORY="Text"
SUMMARY="Allow old apertium transfer rules to be ambiguous"
DESCRIPTION="${SUMMARY}"

inherit git
