--- origsrc/arb-2.21.1/CMakeLists.txt	2021-10-20 18:47:48.000000000 +0900
+++ src/arb-2.21.1/CMakeLists.txt	2021-10-24 20:38:18.718979500 +0900
@@ -148,7 +148,11 @@ foreach (FOLDER ${FOLDERS})
     set(HEADERS ${HEADERS} ${FOLDER}.h)
 endforeach ()
 
-install(FILES ${HEADERS} DESTINATION include)
+if (CYGWIN)
+    install(FILES ${HEADERS} DESTINATION include/arb)
+else()
+    install(FILES ${HEADERS} DESTINATION include)
+endif()
 
 if (BUILD_TESTING)
     enable_testing()
@@ -163,3 +167,11 @@ if (BUILD_TESTING)
     endforeach ()
 endif ()
 
+if (CYGWIN)
+    set(prefix ${CMAKE_INSTALL_PREFIX})
+    set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+    set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+    set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+    configure_file(${CMAKE_SOURCE_DIR}/arb.pc.in ${CMAKE_BINARY_DIR}/arb.pc @ONLY)
+    install(FILES ${CMAKE_BINARY_DIR}/arb.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
+endif()
--- origsrc/arb-2.21.1/arb.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/arb-2.21.1/arb.pc.in	2021-10-24 20:38:18.722004400 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: arb
+Description: C library for arbitrary-precision interval arithmetic
+Version: @ARB_VERSION@
+Libs: -L${libdir} -larb -lflint -lgmp -lmpfr
+Cflags: -I${includedir}
--- origsrc/arb-2.21.1/configure	2021-10-20 18:47:48.000000000 +0900
+++ src/arb-2.21.1/configure	2021-10-24 20:38:18.727005200 +0900
@@ -10,7 +10,7 @@
 # arb     => soname
 # 2.7.0   => 0.0.0
 ARB_MAJOR=2
-ARB_MINOR=12
+ARB_MINOR=21
 ARB_PATCH=1
 
 PREFIX="/usr/local"
