--- origsrc/arb-2.22.0/CMakeLists.txt	2022-01-16 04:09:19.000000000 +0900
+++ src/arb-2.22.0/CMakeLists.txt	2022-01-24 17:28:30.178917600 +0900
@@ -148,7 +148,11 @@ foreach (FOLDER ${FOLDERS})
     set(HEADERS ${HEADERS} ${FOLDER}.h)
 endforeach ()
 
-install(FILES ${HEADERS} DESTINATION include)
+if (CYGWIN)
+    install(FILES ${HEADERS} DESTINATION include/arb)
+else()
+    install(FILES ${HEADERS} DESTINATION include)
+endif()
 
 if (BUILD_TESTING)
     enable_testing()
@@ -163,3 +167,11 @@ if (BUILD_TESTING)
     endforeach ()
 endif ()
 
+if (CYGWIN)
+  set(prefix ${CMAKE_INSTALL_PREFIX})
+  set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+  set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+  set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+  configure_file(${CMAKE_SOURCE_DIR}/arb.pc.in ${CMAKE_BINARY_DIR}/arb.pc @ONLY)
+  install(FILES ${CMAKE_BINARY_DIR}/arb.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
+endif()
--- origsrc/arb-2.22.0/arb.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/arb-2.22.0/arb.pc.in	2022-01-24 17:29:33.042517500 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: arb
+Description: C library for arbitrary-precision interval arithmetic
+Version: @ARB_VERSION@
+Libs: -L${libdir} -larb -lflint -lgmp -lmpfr
+Cflags: -I${includedir}
--- origsrc/arb-2.22.0/configure	2022-01-16 04:09:19.000000000 +0900
+++ src/arb-2.22.0/configure	2022-01-24 16:01:57.833525600 +0900
@@ -10,7 +10,7 @@
 # arb     => soname
 # 2.7.0   => 0.0.0
 ARB_MAJOR=2
-ARB_MINOR=13
+ARB_MINOR=22
 ARB_PATCH=0
 
 PREFIX="/usr/local"
