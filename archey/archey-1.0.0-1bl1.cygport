HOMEPAGE="https://github.com/fd00/${PN}-cygwin"
SRC_URI="https://github.com/fd00/${PN}-cygwin/archive/v${PV}.tar.gz"
SRC_DIR="${PN}-cygwin-${PV}"

CATEGORY="Utils"
SUMMARY="Archey script clone for Cygwin"
DESCRIPTION="${SUMMARY}"

src_compile()
{
	:
}

src_install()
{
	cd ${S}
	dobin ${PN}
}
