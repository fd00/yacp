--- origsrc/asmjit/CMakeLists.txt	2025-09-14 09:06:06.000000000 +0900
+++ src/asmjit/CMakeLists.txt	2025-09-14 09:36:37.579279600 +0900
@@ -203,6 +203,9 @@ function(asmjit_add_target target target
     add_executable(${target} ${X_SOURCES})
   else()
     add_library(${target} ${target_type} ${X_SOURCES})
+    if (DEFINED SOVERSION)
+      set_target_properties(${target} PROPERTIES SOVERSION ${SOVERSION})
+    endif()
   endif()
 
   set_target_properties(${target}
@@ -752,3 +755,9 @@ if (NOT ASMJIT_EMBED)
 
   endif()
 endif()
+set(prefix ${CMAKE_INSTALL_PREFIX})
+set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+configure_file(${CMAKE_SOURCE_DIR}/asmjit.pc.in ${CMAKE_BINARY_DIR}/asmjit.pc @ONLY)
+install(FILES ${CMAKE_BINARY_DIR}/asmjit.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/asmjit/asmjit.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/asmjit/asmjit.pc.in	2025-09-14 09:36:37.553144100 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: asmjit
+Description: Low-latency machine code generation
+Version: @VERSION@
+Libs: -L${libdir} -lasmjit
+Cflags: -I${includedir}
--- origsrc/asmjit/src/asmjit/core/api-build_p.h	2025-09-14 09:06:06.000000000 +0900
+++ src/asmjit/src/asmjit/core/api-build_p.h	2025-09-14 09:15:39.515903500 +0900
@@ -40,7 +40,8 @@
       defined(__DragonFly__) || \
       defined(__FreeBSD__  ) || \
       defined(__NetBSD__   ) || \
-      defined(__OpenBSD__  )
+      defined(__OpenBSD__  ) || \
+      defined(__CYGWIN__   )
     #define ASMJIT_FILE64_API(NAME) NAME
   #else
     #define ASMJIT_FILE64_API(NAME) NAME##64
