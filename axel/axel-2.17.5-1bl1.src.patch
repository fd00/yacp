--- origsrc/axel-2.17.5/doc/Makefile.am	2019-07-24 02:53:03.000000000 +0900
+++ src/axel-2.17.5/doc/Makefile.am	2019-08-02 18:04:59.398866800 +0900
@@ -7,7 +7,7 @@ dist_man_MANS = doc/axel.1
 PACKAGE_DESC = lightweight command line download accelerator
 
 .txt.1:
-	read _ P_DATE _ < VERSION && \
+	read _ P_DATE _ < $(top_srcdir)/VERSION && \
 	tmp=$$(mktemp tmpXXXXXX) && \
 	> "$$tmp" \
 	txt2man -t "$(<:.txt=)" -s 1 \
@@ -15,4 +15,5 @@ PACKAGE_DESC = lightweight command line
 		-P "$(PACKAGE_NAME)" \
 		-r "$(PACKAGE_NAME)-$(PACKAGE_VERSION)" \
 		-v "$(PACKAGE_DESC)" $< &&\
+	mkdir -p doc && \
 	mv "$$tmp" $@ || { rm -f "$$tmp"; exit 1; }
