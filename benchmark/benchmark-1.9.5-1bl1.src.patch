--- origsrc/benchmark-1.9.5/CMakeLists.txt	2026-01-21 22:16:48.000000000 +0900
+++ src/benchmark-1.9.5/CMakeLists.txt	2026-03-15 16:57:20.561854300 +0900
@@ -99,6 +99,7 @@ list(APPEND CMAKE_MODULE_PATH "${CMAKE_C
 list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/cmake")
 
 
+if (EXISTS ${PROJECT_SOURCE_DIR}/.git)
 # Read the git tags to determine the project version
 include(GetGitVersion)
 get_git_version(GIT_VERSION)
@@ -110,6 +111,9 @@ if ("${GIT_VERSION}" STREQUAL "v0.0.0")
 else()
   set(VERSION "${GIT_VERSION}")
 endif()
+else()
+  set(VERSION "${PROJECT_VERSION}")
+endif()
 
 # Normalize version: drop "v" prefix, replace first "-" with ".",
 # drop everything after second "-" (including said "-").
