--- origsrc/binaryen-version_125/CMakeLists.txt	2025-11-20 07:45:33.000000000 +0900
+++ src/binaryen-version_125/CMakeLists.txt	2025-12-31 19:59:46.061155400 +0900
@@ -469,6 +469,9 @@ if(BUILD_STATIC_LIB)
 else()
   message(STATUS "Building libbinaryen as shared library.")
   add_library(binaryen SHARED)
+  if (DEFINED SOVERSION)
+    set_target_properties(binaryen PROPERTIES SOVERSION ${SOVERSION})
+  endif()
   if(LINUX)
     # Disable interposition and resolve Binaryen symbols locally.
     add_link_flag("-Bsymbolic")
@@ -628,3 +631,10 @@ if(EMSCRIPTEN)
 endif()
 
 configure_file(scripts/binaryen-lit.in ${CMAKE_BINARY_DIR}/bin/binaryen-lit @ONLY)
+
+set(prefix ${CMAKE_INSTALL_PREFIX})
+set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+configure_file(${CMAKE_SOURCE_DIR}/binaryen.pc.in ${CMAKE_BINARY_DIR}/binaryen.pc @ONLY)
+install(FILES ${CMAKE_BINARY_DIR}/binaryen.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/binaryen-version_125/binaryen.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/binaryen-version_125/binaryen.pc.in	2025-12-31 19:59:58.608103400 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: binaryen
+Description: Compiler infrastructure and toolchain library for WebAssembly
+Version: @PROJECT_VERSION@
+Libs: -L${libdir} -lbinaryen
+Cflags: -I${includedir}
