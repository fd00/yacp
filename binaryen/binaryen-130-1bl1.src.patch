--- origsrc/binaryen-version_130/CMakeLists.txt	2026-06-02 06:00:57.000000000 +0900
+++ src/binaryen-version_130/CMakeLists.txt	2026-07-15 21:31:07.223817800 +0900
@@ -455,6 +455,10 @@ endif()
 # This will be either be STATIC or SHARED depending on BUILD_SHARED_LIBS
 add_library(binaryen)
 
+if (DEFINED SOVERSION)
+  set_target_properties(binaryen PROPERTIES SOVERSION ${SOVERSION})
+endif()
+
 if(BUILD_SHARED_LIBS)
   add_definitions(-DBUILD_SHARED_LIBS)
   message(STATUS "Building libbinaryen as shared library.")
@@ -574,3 +578,10 @@ if(EMSCRIPTEN)
 endif()
 
 configure_file(scripts/binaryen-lit.in ${CMAKE_BINARY_DIR}/bin/binaryen-lit @ONLY)
+
+set(prefix ${CMAKE_INSTALL_PREFIX})
+set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+configure_file(${CMAKE_SOURCE_DIR}/binaryen.pc.in ${CMAKE_BINARY_DIR}/binaryen.pc @ONLY)
+install(FILES ${CMAKE_BINARY_DIR}/binaryen.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/binaryen-version_130/binaryen.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/binaryen-version_130/binaryen.pc.in	2026-07-15 21:30:54.604880000 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: binaryen
+Description: Compiler infrastructure and toolchain library for WebAssembly
+Version: @PROJECT_VERSION@
+Libs: -L${libdir} -lbinaryen
+Cflags: -I${includedir}
