--- origsrc/binaryen-version_96/CMakeLists.txt	2020-08-14 11:28:32.000000000 +0900
+++ src/binaryen-version_96/CMakeLists.txt	2020-08-31 08:28:51.973328500 +0900
@@ -210,8 +210,10 @@ else()
   add_compile_flag("-Wno-implicit-int-float-conversion")
   add_compile_flag("-Wno-unknown-warning-option")
   add_compile_flag("-Wswitch") # we explicitly expect this in the code
-  if(WIN32)
+  if(WIN32 OR CYGWIN)
     add_compile_flag("-D_GNU_SOURCE")
+  endif()
+  if(WIN32)
     add_link_flag("-Wl,--stack,8388608")
   elseif(NOT EMSCRIPTEN)
     add_compile_flag("-fPIC")
@@ -301,6 +303,9 @@ else()
   message(STATUS "Building libbinaryen as shared library.")
   add_library(binaryen SHARED ${binaryen_SOURCES} ${binaryen_objs})
 endif()
+if(CYGWIN)
+  set_target_properties(binaryen PROPERTIES SOVERSION 0)
+endif()
 install(TARGETS binaryen
   RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
   LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
@@ -399,3 +404,10 @@ add_test(NAME exec-unit
          COMMAND bin/wasm-opt test/unit.wat --fuzz-exec)
 add_test(NAME exec-hello
          COMMAND bin/wasm-opt test/hello_world.wat --fuzz-exec)
+
+SET(prefix ${CMAKE_INSTALL_PREFIX})
+SET(exec_prefix ${CMAKE_INSTALL_PREFIX})
+SET(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+SET(includedir ${CMAKE_INSTALL_PREFIX}/include)
+CONFIGURE_FILE(${CMAKE_SOURCE_DIR}/binaryen.pc.in ${CMAKE_BINARY_DIR}/binaryen.pc @ONLY)
+INSTALL(FILES ${CMAKE_BINARY_DIR}/binaryen.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/binaryen-version_96/binaryen.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/binaryen-version_96/binaryen.pc.in	2020-08-31 07:46:12.841070200 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: binaryen
+Description: Compiler infrastructure and toolchain library for WebAssembly
+Version: @VERSION@
+Libs: -L${libdir} -lbinaryen
+Cflags: -I${includedir}
