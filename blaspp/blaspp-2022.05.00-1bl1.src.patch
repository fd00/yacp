--- origsrc/blaspp-2022.05.00/CMakeLists.txt	2022-05-31 16:04:48.000000000 +0900
+++ src/blaspp-2022.05.00/CMakeLists.txt	2022-10-06 16:15:59.072670200 +0900
@@ -353,6 +353,10 @@ set_target_properties( blaspp PROPERTIES
     WINDOWS_EXPORT_ALL_SYMBOLS ON
 )
 
+if (DEFINED SOVERSION)
+    set_target_properties(blaspp PROPERTIES SOVERSION "${SOVERSION}")
+endif()
+
 if (CMAKE_VERSION VERSION_GREATER_EQUAL 3.15)
     # Conditionally add -Wall. See CMake tutorial.
     set( gcc_like_cxx "$<COMPILE_LANG_AND_ID:CXX,ARMClang,AppleClang,Clang,GNU>" )
@@ -439,6 +443,8 @@ endif()
 include( GNUInstallDirs )
 if (WIN32)
     set( install_configdir "blaspp" )
+elseif(CYGWIN)
+    set( install_configdir "${CMAKE_INSTALL_LIBDIR}/cmake/blaspp" )
 else()
     set( install_configdir "${CMAKE_INSTALL_LIBDIR}/blaspp" )
 endif()
@@ -494,3 +500,10 @@ install(
           "${CMAKE_CURRENT_BINARY_DIR}/blasppConfigVersion.cmake"
     DESTINATION "${install_configdir}"
 )
+
+set(prefix ${CMAKE_INSTALL_PREFIX})
+set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+configure_file(${CMAKE_SOURCE_DIR}/blaspp.pc.in ${CMAKE_BINARY_DIR}/blaspp.pc @ONLY)
+install(FILES ${CMAKE_BINARY_DIR}/blaspp.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/blaspp-2022.05.00/blaspp.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/blaspp-2022.05.00/blaspp.pc.in	2022-10-06 17:27:59.030359800 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: blaspp
+Description: C++ API for the Basic Linear Algebra Subroutines
+Version: @VERSION@
+Libs: -L${libdir} -lblaspp
+Cflags: -I${includedir}
