--- origsrc/blend2d/CMakeLists.txt	2025-09-14 10:08:13.000000000 +0900
+++ src/blend2d/CMakeLists.txt	2025-09-14 10:34:42.929331100 +0900
@@ -538,6 +538,9 @@ function(blend2d_add_target target targe
     set_target_properties(${target} PROPERTIES VS_DEBUGGER_WORKING_DIRECTORY "$(TargetDir).")
   else()
     add_library(${target} ${target_type} ${X_SOURCES})
+    if (DEFINED SOVERSION)
+      set_target_properties(${target} PROPERTIES SOVERSION ${SOVERSION})
+    endif()
   endif()
 
   set_target_properties(${target}
@@ -1251,3 +1254,9 @@ if (NOT BLEND2D_EMBED)
 
   endif()
 endif()
+set(prefix ${CMAKE_INSTALL_PREFIX})
+set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+configure_file(${CMAKE_SOURCE_DIR}/blend2d.pc.in ${CMAKE_BINARY_DIR}/blend2d.pc @ONLY)
+install(FILES ${CMAKE_BINARY_DIR}/blend2d.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/blend2d/blend2d.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/blend2d/blend2d.pc.in	2025-09-14 10:34:42.901643000 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: blend2d
+Description: 2D Vector Graphics Engine Powered by a JIT Compiler
+Version: @VERSION@
+Libs: -L${libdir} -lblend2d
+Cflags: -I${includedir}
--- origsrc/blend2d/src/blend2d/api-build_p.h	2025-09-14 10:08:13.000000000 +0900
+++ src/blend2d/src/blend2d/api-build_p.h	2025-09-14 10:12:24.090691800 +0900
@@ -152,7 +152,8 @@ extern "C" void __tsan_release(void *add
       defined(__DragonFly__) || \
       defined(__FreeBSD__  ) || \
       defined(__NetBSD__   ) || \
-      defined(__OpenBSD__  )
+      defined(__OpenBSD__  ) || \
+      defined(__CYGWIN__   )
     #define BL_FILE64_API(NAME) NAME
   #else
     #define BL_FILE64_API(NAME) NAME##64
