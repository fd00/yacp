DESCRIPTION="C library that resolves names asynchronously"
HOMEPAGE="http://${PN}.haxx.se/"
SRC_URI="http://${PN}.haxx.se/${P}.tar.gz"

PKG_NAMES="${PN} libcares2 libcares-devel"
#c_ares_CONTENTS=""
libcares2_CONTENTS="usr/bin usr/share/doc"
libcares_devel_CONTENTS="usr/include usr/lib usr/share/man"

DOCS="README.cares RELEASE-NOTES"

DIFF_EXCLUDES="ares_build.h ares_config.h"
