HOMEPAGE="http://${PN}.sf.net/"
SRC_URI="mirror://sourceforge/${PN}/${P}.tar.gz"
PATCH_URI="
	http://pkgs.fedoraproject.org/cgit/${PN}.git/plain/${P}-compilerWarnings.patch
	http://pkgs.fedoraproject.org/cgit/${PN}.git/plain/${P}-manual.patch
"

CATEGORY="Archive"
SUMMARY="CloneCD image to ISO image file converter"
DESCRIPTION="The ccd2iso project converts CD backup files created using the non-free
CloneCD program to a format understood by most Free Software CD writing
programs."
