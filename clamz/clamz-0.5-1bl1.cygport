DESCRIPTION="Downloader for the Amazon.com MP3 music store"
HOMEPAGE="http://${PN}.googlecode.com/"
SRC_URI="http://${PN}.googlecode.com/files/${P}.tar.gz"

src_install()
{
	cd ${B}
	cyginstall UPDATE_MIME_DATABASE=: UPDATE_DESKTOP_DATABASE=:
}
