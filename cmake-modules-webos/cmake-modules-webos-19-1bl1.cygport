HOMEPAGE="https://github.com/FreeWebOS/${PN}"
SRC_URI="https://github.com/FreeWebOS/${PN}/archive/submissions/${PV}.tar.gz"
SRC_DIR="${PN}-submissions-${PV}"

CATEGORY="Devel"
SUMMARY="CMake modules needed to build Open webOS components"
DESCRIPTION="cmake-modules-webos extends your CMake installation with a set of functions
and macros that are required to build the majority of those Open webOS
components which use CMake for configuration."

inherit cmake

DOCS="
	*.md
"
