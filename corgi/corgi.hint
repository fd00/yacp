category: Libs
requires: cygwin libcorgi0
sdesc: "Regular expression library for C (utilities)"
ldesc: "corgi is a regular expression library for C.
 * corgi accepts UTF-32 strings.
 * Based on Python's regular expression's engine (it seems fast).
 * Open source"
