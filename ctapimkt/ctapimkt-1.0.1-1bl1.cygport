DESCRIPTION="Library and program to read KVK and eGK"
HOMEPAGE="http://ctapi-mkt.sourceforge.net/"
SRC_URI="mirror://sourceforge/ctapi-mkt/${P}.tar.gz"

CYGCONF_ARGS="--disable-static"

PKG_NAMES="${PN} lib${PN}0 lib${PN}-devel"
ctapimkt_CONTENTS="usr/bin/*.exe usr/share"
libctapimkt0_CONTENTS="usr/bin/*.dll"
libctapimkt_devel_CONTENTS="usr/include usr/lib"
