--- origsrc/doctest-2.5.3/doctest/doctest.h	2026-07-06 21:58:29.000000000 +0000
+++ src/doctest-2.5.3/doctest/doctest.h	2026-08-15 01:52:04.933879900 +0000
@@ -315,6 +315,9 @@ DOCTEST_SUPPRESS_PUBLIC_WARNINGS_PUSH
 #elif defined(__wasi__)
 #define DOCTEST_PLATFORM_WASI
 
+#elif defined(__CYGWIN__)
+#define DOCTEST_PLATFORM_CYGWIN
+
 #else // defined(linux) || defined(__linux) // defined(__linux__)
 #define DOCTEST_PLATFORM_LINUX
 #endif // DOCTEST_PLATFORM
--- origsrc/doctest-2.5.3/doctest/parts/public/debugger.h	2026-07-06 21:58:29.000000000 +0000
+++ src/doctest-2.5.3/doctest/parts/public/debugger.h	2026-08-15 01:52:04.933879900 +0000
@@ -18,6 +18,10 @@
 #define DOCTEST_BREAK_INTO_DEBUGGER() raise(SIGTRAP)
 #endif
 
+#elif defined(DOCTEST_PLATFORM_CYGWIN)
+#include <signal.h>
+#define DOCTEST_BREAK_INTO_DEBUGGER() raise(SIGTRAP)
+
 #elif defined(DOCTEST_PLATFORM_MAC)
 #if defined(__x86_64) || defined(__x86_64__) || defined(__amd64__) || defined(__i386)
 #define DOCTEST_BREAK_INTO_DEBUGGER() __asm__("int $3\n" ::) // NOLINT(hicpp-no-assembler)
--- origsrc/doctest-2.5.3/doctest.pc.in	2026-07-06 21:58:29.000000000 +0000
+++ src/doctest-2.5.3/doctest.pc.in	2026-08-15 01:52:04.935898700 +0000
@@ -1,7 +1,7 @@
-prefix="@CMAKE_INSTALL_PREFIX@"
-exec_prefix="${prefix}"
-libdir="${prefix}/lib"
-includedir="${prefix}/include"
+prefix=@CMAKE_INSTALL_PREFIX@
+exec_prefix=${prefix}
+libdir=${prefix}/lib
+includedir=${prefix}/include
 
 Name: @PROJECT_NAME@
 Description: The fastest feature-rich C++11/14/17/20/23 single-header testing framework
--- origsrc/doctest-2.5.3/tests/doctest/test_debugger.cpp	2026-07-06 21:58:29.000000000 +0000
+++ src/doctest-2.5.3/tests/doctest/test_debugger.cpp	2026-08-15 01:52:04.937914700 +0000
@@ -3,7 +3,7 @@
 #if defined(DOCTEST_PLATFORM_WINDOWS)
 #include <doctest/parts/private/ext/windows.h>
 
-#elif defined(DOCTEST_PLATFORM_LINUX)
+#elif defined(DOCTEST_PLATFORM_LINUX) || defined(DOCTEST_PLATFORM_CYGWIN)
 #include <csignal>
 
 #else
@@ -39,7 +39,7 @@ inline void setup() {
     REQUIRE(handle == nullptr);
     handle = AddVectoredExceptionHandler(1, veh);
 
-#elif defined(DOCTEST_PLATFORM_LINUX)
+#elif defined(DOCTEST_PLATFORM_LINUX) || defined(DOCTEST_PLATFORM_CYGWIN)
     DOCTEST_CLANG_SUPPRESS_WARNING_WITH_PUSH("-Wdisabled-macro-expansion") /* sa_handler is a self-referential macro */
     struct sigaction sa = {};
     sa.sa_handler = [](int) { signal_caught = true; };
@@ -56,7 +56,7 @@ inline void cleanup() {
         RemoveVectoredExceptionHandler(handle);
         handle = nullptr;
     }
-#elif defined(DOCTEST_PLATFORM_LINUX)
+#elif defined(DOCTEST_PLATFORM_LINUX) || defined(DOCTEST_PLATFORM_CYGWIN)
 #if DOCTEST_CLANG >= DOCTEST_COMPILER(5, 0, 0)
     DOCTEST_CLANG_SUPPRESS_WARNING_WITH_PUSH(
         "-Wzero-as-null-pointer-constant"
--- origsrc/doctest-2.5.3/tests/doctest/test_platform.cpp	2026-07-06 21:58:29.000000000 +0000
+++ src/doctest-2.5.3/tests/doctest/test_platform.cpp	2026-08-15 01:52:04.937914700 +0000
@@ -35,6 +35,12 @@ constexpr auto linux_ = true;
 constexpr auto linux_ = false;
 #endif // DOCTEST_PLATFORM_LINUX
 
+#if defined(DOCTEST_PLATFORM_CYGWIN)
+constexpr auto cygwin = true;
+#else
+constexpr auto cygwin = false;
+#endif // DOCTEST_PLATFORM_CYGWIN
+
 } // namespace platform
 
 TEST_CASE("Exactly one platform is detected") {
@@ -43,6 +49,7 @@ TEST_CASE("Exactly one platform is detec
     CAPTURE(platform::windows);
     CAPTURE(platform::wasi);
     CAPTURE(platform::linux_);
+    CAPTURE(platform::cygwin);
 
     const auto detected = std::multiset<bool>{
         platform::mac,
@@ -50,6 +57,7 @@ TEST_CASE("Exactly one platform is detec
         platform::windows,
         platform::wasi,
         platform::linux_,
+        platform::cygwin,
     };
 
     CHECK(detected.count(true) == 1);
@@ -69,4 +77,5 @@ TEST_CASE("Detected platform matches wha
     CHECK(platform::windows == (cmake_system_name == "Windows"));
     CHECK(platform::wasi == (cmake_system_name == "Emscripten"));
     CHECK(platform::linux_ == (cmake_system_name == "Linux"));
+    CHECK(platform::cygwin == (cmake_system_name == "CYGWIN"));
 }
