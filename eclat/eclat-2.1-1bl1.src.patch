--- origsrc/eclat-2.1/lib/ldapmap.c	2021-12-01 19:02:28.000000000 +0900
+++ src/eclat-2.1/lib/ldapmap.c	2026-01-06 12:56:03.335209300 +0900
@@ -293,7 +293,7 @@ parse_ldap_uri(const char *uri)
 }
 
 static LDAP *
-ldap_connect(int dbg, struct ldap_map *map)
+_ldap_connect(int dbg, struct ldap_map *map)
 {
 	int rc;
 	char *ldapuri = NULL;
@@ -512,7 +512,7 @@ ldap_map_open(int dbg, void *data)
 		map->bindpw = eclat_getans("User password", NULL, 1);
 	free(user);
 	
-	map->ld = ldap_connect(dbg, map);
+	map->ld = _ldap_connect(dbg, map);
 	if (!map->ld)
 		return eclat_map_failure;
 	if (ldap_bind(dbg, map))
