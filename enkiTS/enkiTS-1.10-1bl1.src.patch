--- origsrc/enkiTS-1.10/CMakeLists.txt	2021-07-20 19:28:57.000000000 +0900
+++ src/enkiTS-1.10/CMakeLists.txt	2021-08-13 08:15:57.943519200 +0900
@@ -60,7 +60,12 @@ if( UNIX )
 endif()
 
 if( ENKITS_INSTALL )
-    install(TARGETS enkiTS DESTINATION "${CMAKE_INSTALL_PREFIX}/lib/enkiTS")
+    if (CYGWIN)
+        install(TARGETS enkiTS RUNTIME DESTINATION bin ARCHIVE DESTINATION lib)
+        set_target_properties(enkiTS PROPERTIES SOVERSION 0)
+    else()
+        install(TARGETS enkiTS DESTINATION "${CMAKE_INSTALL_PREFIX}/lib/enkiTS")
+    endif()
     install(FILES ${ENKITS_HEADERS} DESTINATION "${CMAKE_INSTALL_PREFIX}/include/enkiTS")
 endif()
 
@@ -143,3 +148,10 @@ if( ENKITS_BUILD_C_INTERFACE )
 endif()    
 
 endif()
+
+SET(prefix ${CMAKE_INSTALL_PREFIX})
+SET(exec_prefix ${CMAKE_INSTALL_PREFIX})
+SET(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+SET(includedir ${CMAKE_INSTALL_PREFIX}/include)
+CONFIGURE_FILE(${CMAKE_SOURCE_DIR}/enkiTS.pc.in ${CMAKE_BINARY_DIR}/enkiTS.pc @ONLY)
+INSTALL(FILES ${CMAKE_BINARY_DIR}/enkiTS.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/enkiTS-1.10/enkiTS.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/enkiTS-1.10/enkiTS.pc.in	2021-08-13 08:15:57.901496300 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: enkiTS
+Description: C and C++ Task Scheduler for creating parallel programs
+Version: @VERSION@
+Libs: -L${libdir} -lenkiTS
+Cflags: -I${includedir}
