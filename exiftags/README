exiftags
------------------------------------------
The exiftags utility parses a specified JPEG file or, by default, its standard
input, looking for a JPEG APP1 section containing Exif (Exchangeable Image
File) image metadata. The properties contained in these data are then printed
to the standard output. Digital cameras typically add Exif data to the image
files they produce, containing information about the camera and digitized
image.

Runtime requirements:
  cygwin-3.5.0-0.505.g0737bda6ff5a

Build requirements:
(besides corresponding -devel packages)
  binutils-2.41-3
  cygport-0.36.7-1
  gcc-core-11.4.0-1
  make-4.4.1-2

Canonical website:
  https://johnst.org/sw/exiftags/

Canonical download:
  https://johnst.org/sw/exiftags/exiftags-1.01.tar.gz

-------------------------------------------

Build instructions:
  1. unpack exiftags-1.01-X-src.tar.xz
  2. if you use setup to install this src package,
     it will be unpacked under /usr/src automatically
        % cd /usr/src
        % cygport ./exiftags-1.01-X.cygport all

This will create:
  /usr/src/exiftags-1.01-X-src.tar.xz
  /usr/src/exiftags-1.01-X.tar.xz

-------------------------------------------

Files included in the binary package:

(exiftags)
  /usr/bin/exifcom.exe
  /usr/bin/exiftags.exe
  /usr/bin/exiftime.exe
  /usr/share/doc/Cygwin/exiftags.README
  /usr/share/doc/exiftags/CHANGES
  /usr/share/doc/exiftags/README
  /usr/share/man/man1/exifcom.1.gz
  /usr/share/man/man1/exiftags.1.gz
  /usr/share/man/man1/exiftime.1.gz

------------------

Port Notes:

----- version 1.01-1bl1 -----
Initial release by fd0 <https://github.com/fd00/>
