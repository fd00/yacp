--- origsrc/fastq-tools-0.8.3/configure.ac	2020-10-31 01:27:47.000000000 +0900
+++ src/fastq-tools-0.8.3/configure.ac	2025-12-24 16:20:43.419173200 +0900
@@ -17,17 +17,17 @@ m4_ifdef([AC_TYPE_UINT64_T], [AC_TYPE_UI
 m4_ifdef([AC_TYPE_SIZE_T],   [AC_TYPE_SIZE_T])
 m4_ifdef([AC_HEADER_STDBOOL], [AC_HEADER_STDBOOL])
 
-opt_CFLAGS="--std=gnu99 -Wall -Wextra -g -O3"
-dbg_CFLAGS="--std=gnu99 -Wall -Wextra -g -O0"
+#opt_CFLAGS="--std=gnu99 -Wall -Wextra -g -O3"
+#dbg_CFLAGS="--std=gnu99 -Wall -Wextra -g -O0"
 
-AC_ARG_ENABLE([debug],
-              [AS_HELP_STRING([--enable-debug],
-                              [enable debugging info (default is no)])],
-              [], [enable_debug=no])
+#AC_ARG_ENABLE([debug],
+#              [AS_HELP_STRING([--enable-debug],
+#                              [enable debugging info (default is no)])],
+#              [], [enable_debug=no])
 
-AS_IF([test "x$enable_debug" = xyes],
-      [CFLAGS="$dbg_CFLAGS"],
-      [CFLAGS="$opt_CFLAGS"])
+#AS_IF([test "x$enable_debug" = xyes],
+#      [CFLAGS="$dbg_CFLAGS"],
+#      [CFLAGS="$opt_CFLAGS"])
 
 AC_DEFINE(_FILE_OFFSET_BITS, 64)
 AC_DEFINE(_POSIX_SOURCE) # needed for fileno
--- origsrc/fastq-tools-0.8.3/tests/Makefile.am	2020-10-31 01:27:47.000000000 +0900
+++ src/fastq-tools-0.8.3/tests/Makefile.am	2025-12-24 16:14:53.444124300 +0900
@@ -3,4 +3,4 @@ check_PROGRAMS = random_fastq cat_fastq
 
 TESTS = parse_fastq
 
-
+AM_CPPFLAGS = -I$(top_builddir)/src
