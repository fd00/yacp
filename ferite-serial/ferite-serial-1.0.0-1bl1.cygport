HOMEPAGE="https://sourceforge.net/projects/ferite/"
SRC_URI="mirror://sourceforge/ferite/serial-ferite-module-${PV}.tar.gz"
SRC_DIR="serial-ferite-module-${PV}"

CATEGORY="Libs"
SUMMARY="Ferite serial module"
DESCRIPTION="This is a Ferite serial interface module. Currently it only supports BSD
termios but the API is designed to be generic enough to support other serial
port systems without needing to change the API too dramatically."

BUILD_REQUIRES="libferite-devel"
