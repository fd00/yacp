DESCRIPTION="Cross-platform C module for detecting the user's language"
HOMEPAGE="http://ohnopub.net/~ohnobinki/${PN}/"
SRC_URI="ftp://ohnopub.net/mirror/${P}.tar.bz2"

CYGCONF_ARGS="--disable-static"

PKG_NAMES="${PN} lib${PN}0 lib${PN}-devel"
findlocale_CONTENTS="usr/share/doc"
libfindlocale0_CONTENTS="usr/bin"
libfindlocale_devel_CONTENTS="usr/include usr/lib"
