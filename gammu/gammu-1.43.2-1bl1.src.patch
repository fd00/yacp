--- origsrc/gammu-1.43.2/CMakeLists.txt	2026-02-10 00:20:04.000000000 +0900
+++ src/gammu-1.43.2/CMakeLists.txt	2026-06-24 22:50:15.478902000 +0900
@@ -528,12 +528,12 @@ if(CMAKE_COMPILER_IS_GNUCC OR CMAKE_COMP
     endif (WIN32 AND NOT CYGWIN AND BUILD_SHARED_LIBS)
 
     MACRO_TUNE_LINKER("-Wl,--as-needed")
-    if (NOT WIN32)
+    if (NOT WIN32 AND NOT CYGWIN)
         set (PIC ON CACHE BOOL "Compile PIC code")
         if (PIC)
             MACRO_TUNE_COMPILER("-fPIC")
         endif (PIC)
-    endif (NOT WIN32)
+    endif (NOT WIN32 AND NOT CYGWIN)
     # Specs for MinGW to change msvcrt
     # This is not supported by MinGW and is broken currently
 #    if (CMAKE_COMPILER_IS_MINGW)
--- origsrc/gammu-1.43.2/cmake/templates/gammu-config.h.cmake	2026-02-10 00:20:04.000000000 +0900
+++ src/gammu-1.43.2/cmake/templates/gammu-config.h.cmake	2026-06-24 22:50:15.486260900 +0900
@@ -384,7 +384,7 @@
 #define GAMMU_DATA_PATH "@CMAKE_INSTALL_PREFIX@/@INSTALL_DATA_DIR@"
 
 /* Most winapi crap can be used as well from Cygwin */
-#if defined(WIN32) || defined(__CYGWIN__)
+#if defined(WIN32)
 #define HAVE_WINDOWS_SERVICE
 #define HAVE_WINDOWS_EVENT_LOG
 #endif
--- origsrc/gammu-1.43.2/smsd/services/mysql.c	2026-02-10 00:20:04.000000000 +0900
+++ src/gammu-1.43.2/smsd/services/mysql.c	2026-06-25 06:10:56.653882400 +0900
@@ -10,6 +10,8 @@
 #include <time.h>
 #include <stdlib.h>
 #include <assert.h>
+#include <mysql/mysql.h>
+#include <mysql/errmsg.h>
 
 #ifdef WIN32
 #include <windows.h>
