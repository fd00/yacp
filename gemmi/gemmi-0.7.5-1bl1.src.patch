--- origsrc/gemmi-0.7.5/CMakeLists.txt	2026-03-02 03:06:09.000000000 +0000
+++ src/gemmi-0.7.5/CMakeLists.txt	2026-08-18 19:37:03.979238900 +0000
@@ -266,6 +266,10 @@ else()
   endif()
 endif()
 
+if (DEFINED SOVERSION)
+  set_target_properties(gemmi_cpp PROPERTIES SOVERSION "${SOVERSION}")
+endif()
+
 
 ### programs from prog/ ###
 
@@ -626,3 +630,10 @@ if (USE_PYTHON)
             DESTINATION "${Python_SITEARCH}/gemmi" COMPONENT py)
   endif()
 endif()
+
+set(prefix ${CMAKE_INSTALL_PREFIX})
+set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+configure_file(${CMAKE_SOURCE_DIR}/gemmi.pc.in ${CMAKE_BINARY_DIR}/gemmi.pc @ONLY)
+install(FILES ${CMAKE_BINARY_DIR}/gemmi.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/gemmi-0.7.5/gemmi.pc.in	1970-01-01 00:00:00.000000000 +0000
+++ src/gemmi-0.7.5/gemmi.pc.in	2026-08-18 19:37:03.981253500 +0000
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: gemmi
+Description: Macromolecular crystallography library and utilities
+Version: @PROJECT_VERSION@
+Libs: -L${libdir} -lgemmi_cpp
+Cflags: -I${includedir}
--- origsrc/gemmi-0.7.5/include/gemmi/fail.hpp	2026-03-02 03:06:09.000000000 +0000
+++ src/gemmi-0.7.5/include/gemmi/fail.hpp	2026-08-18 19:37:03.981253500 +0000
@@ -39,7 +39,7 @@
 #  define GEMMI_UNLIKELY(x) (x)
 #endif
 
-#if defined(_WIN32)
+#if defined(_WIN32) || defined(__CYGWIN__)
 # if defined(GEMMI_SHARED)
 #  if defined(GEMMI_BUILD)
 #   define GEMMI_DLL __declspec(dllexport)
