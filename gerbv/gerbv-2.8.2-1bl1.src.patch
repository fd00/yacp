--- origsrc/gerbv-2.8.2/Makefile.am	2021-12-19 20:08:36.000000000 +0900
+++ src/gerbv-2.8.2/Makefile.am	2022-04-17 10:17:18.863877300 +0900
@@ -25,7 +25,6 @@ SUBDIRS = \
 	share/glib-2.0/schemas \
 	desktop \
 	doc man \
-	po \
 	example \
 	test \
 	win32
--- origsrc/gerbv-2.8.2/configure.ac	2021-12-19 20:08:36.000000000 +0900
+++ src/gerbv-2.8.2/configure.ac	2022-04-17 10:48:20.136285300 +0900
@@ -391,7 +391,9 @@ AC_CHECK_FUNCS(strlwr)
 AC_CHECK_FUNCS(realpath canonicalize_file_name)
 libiberty_NEED_DECLARATION(canonicalize_file_name)
 
-AC_SUBST([GTK_CFLAGS_ISYSTEM], ['$(subst -I/usr/include/gtk-2.0,-isystem /usr/include/gtk-2.0,$(GTK_CFLAGS))'])
+# AC_SUBST([GTK_CFLAGS_ISYSTEM], ['$(subst -I/usr/include/gtk-2.0,-isystem /usr/include/gtk-2.0,$(GTK_CFLAGS))'])
+GTK_CFLAGS_ISYSTEM="${GTK_CFLAGS}"
+AC_SUBST(GTK_CFLAGS_ISYSTEM)
 
 CFLAGS="$CFLAGS $GDK_PIXBUF_CFLAGS $GTK_CFLAGS_ISYSTEM $CAIRO_CFLAGS"
 LIBS="$LIBS $GDK_PIXBUF_LIBS $GTK_LIBS $CAIRO_LIBS -lm"
--- origsrc/gerbv-2.8.2/src/run_gerbv.in	2021-12-19 20:08:36.000000000 +0900
+++ src/gerbv-2.8.2/src/run_gerbv.in	2022-04-17 10:56:40.728002200 +0900
@@ -84,6 +84,12 @@ done
 #
 # This hack came from wcalc.
 
+if test X`uname -o` = XCygwin ; then
+	SONAME=cyggerbv-1.dll
+else
+	SONAME=libgerbv.so
+fi
+
 LIBGERBV_DIR=@BUILDDIR@/.libs
 if test "X${LD_LIBRARY_PATH}" = "X" ; then
 	LD_LIBRARY_PATH=${LIBGERBV_DIR}
@@ -102,9 +108,9 @@ export LD_RUN_PATH
 echo "Set LD_RUN_PATH to ${LD_RUN_PATH}"
 
 if test "X${LD_PRELOAD}" = "X" ; then
-	LD_PRELOAD=${LIBGERBV_DIR}/libgerbv.so
+	LD_PRELOAD=${LIBGERBV_DIR}/${SONAME}
 else
-	LD_PRELOAD=${LIBGERBV_DIR}/libgerbv.so:${LD_PRELOAD}
+	LD_PRELOAD=${LIBGERBV_DIR}/${SONAME}:${LD_PRELOAD}
 fi 
 export LD_PRELOAD
 echo "Set LD_PRELOAD to ${LD_PRELOAD}"
--- origsrc/gerbv-2.8.2/utils/git-version-gen.sh	2021-12-19 20:08:36.000000000 +0900
+++ src/gerbv-2.8.2/utils/git-version-gen.sh	2022-04-17 12:58:31.499908000 +0900
@@ -19,6 +19,10 @@ if [ "" == "${PREFIX}" ]; then
 	exit 1
 fi
 
+if [ ! -d ".git" ]; then
+	echo -n "${PREFIX}"
+	exit 0
+fi
 
 # Validate environment
 GIT=`command -v git`
