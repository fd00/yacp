--- origsrc/glfw-3.0.4/src/CMakeLists.txt	2014-01-01 04:06:18.000000000 +0900
+++ src/glfw-3.0.4/src/CMakeLists.txt	2014-11-07 11:54:13.217504100 +0900
@@ -83,6 +83,6 @@ if (BUILD_SHARED_LIBS)
 endif()
 
 if (GLFW_INSTALL)
-    install(TARGETS glfw EXPORT glfwTargets DESTINATION lib${LIB_SUFFIX})
+    install(TARGETS glfw EXPORT glfwTargets RUNTIME DESTINATION bin ARCHIVE DESTINATION lib${LIB_SUFFIX})
 endif()
 
