--- origsrc/gmt-5.4.5/src/CMakeLists.txt	2019-01-03 03:05:21.000000000 +0900
+++ src/gmt-5.4.5/src/CMakeLists.txt	2019-09-26 14:44:12.644611700 +0900
@@ -420,14 +420,23 @@ if (HAVE_ZLIB)
 endif (HAVE_ZLIB)
 
 # set the build version (VERSION) and the API version (SOVERSION)
-set_target_properties (pslib
-	PROPERTIES
-	OUTPUT_NAME postscriptlight
-	PREFIX "${LIB_PREFIX}"
-	IMPORT_PREFIX "${LIB_PREFIX}"
-	VERSION ${GMT_LIB_VERSION}
-	SOVERSION ${GMT_LIB_SOVERSION}
-	DEFINE_SYMBOL "LIBRARY_EXPORTS")
+if (CYGWIN)
+	set_target_properties (pslib
+		PROPERTIES
+		OUTPUT_NAME postscriptlight
+		VERSION ${GMT_LIB_VERSION}
+		SOVERSION ${GMT_LIB_SOVERSION}
+		DEFINE_SYMBOL "LIBRARY_EXPORTS")
+else ()
+	set_target_properties (pslib
+		PROPERTIES
+		OUTPUT_NAME postscriptlight
+		PREFIX "${LIB_PREFIX}"
+		IMPORT_PREFIX "${LIB_PREFIX}"
+		VERSION ${GMT_LIB_VERSION}
+		SOVERSION ${GMT_LIB_SOVERSION}
+		DEFINE_SYMBOL "LIBRARY_EXPORTS")
+endif ()
 
 # If a renaming of the gmtpslpsl dll has been set in ConfigUser.cmake
 if (WIN32 AND PSL_DLL_RENAME)
@@ -558,15 +567,25 @@ if (HAVE_M_LIBRARY)
 endif (HAVE_M_LIBRARY)
 
 # set the build version (VERSION) and the API version (SOVERSION)
-set_target_properties (gmtlib
-	PROPERTIES
-	OUTPUT_NAME gmt
-	PREFIX "${LIB_PREFIX}"
-	IMPORT_PREFIX "${LIB_PREFIX}"
-	VERSION ${GMT_LIB_VERSION}
-	SOVERSION ${GMT_LIB_SOVERSION}
-	LINK_FLAGS "${USER_GMTLIB_LINK_FLAGS}"
-	DEFINE_SYMBOL "LIBRARY_EXPORTS")
+if (CYGWIN)
+	set_target_properties (gmtlib
+		PROPERTIES
+		OUTPUT_NAME gmt
+		VERSION ${GMT_LIB_VERSION}
+		SOVERSION ${GMT_LIB_SOVERSION}
+		LINK_FLAGS "${USER_GMTLIB_LINK_FLAGS}"
+		DEFINE_SYMBOL "LIBRARY_EXPORTS")
+else()
+	set_target_properties (gmtlib
+		PROPERTIES
+		OUTPUT_NAME gmt
+		PREFIX "${LIB_PREFIX}"
+		IMPORT_PREFIX "${LIB_PREFIX}"
+		VERSION ${GMT_LIB_VERSION}
+		SOVERSION ${GMT_LIB_SOVERSION}
+		LINK_FLAGS "${USER_GMTLIB_LINK_FLAGS}"
+		DEFINE_SYMBOL "LIBRARY_EXPORTS")
+endif()
 
 # If a renaming of the gmt dll has been set in ConfigUser.cmake
 if (WIN32 AND GMT_DLL_RENAME)
