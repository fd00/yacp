category: Libs
requires: cygwin libhashit1
sdesc: "Generic hash library implemented in C (licensing & readmes)"
ldesc: "hashit is a generic hash library implemented in C which supports
multiple collision handling methods. I did it for fun, but some users are
using it in Real World (tm) applications, so it works ;-)."
