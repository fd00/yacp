hffzip
------------------------------------------
HFFzip is a file compressor for Linux and FreeBSD based upon Huffman coding.
The Huffman algorithm uses a binary tree, and has complexity O(N*N) for
compression and O(NlogN) for decompression. Although this file compressor is
not so good as other compression programs (like gzip), HFFzip is right for
embedded systems, because of its little size and the simple algorithm used.

Runtime requirements:
  cygwin-1.7.11-1

Build requirements:
(besides corresponding -devel packages)
  binutils-2.22.51-1
  cygport-0.10.8-1
  gcc4-core-4.5.3-3
  make-3.82.90-1

Canonical website:
  http://retis.sssup.it/~scordino/code/hffzip.html

Canonical download:
  http://retis.sssup.it/~scordino/code/hffzip1.01.tar.gz

-------------------------------------------

Build instructions:
  unpack hffzip-1.01-X-src.tar.bz2
    if you use setup to install this src package, it will be
	 unpacked under /usr/src automatically
  cd /usr/src
  cygport ./hffzip-1.01-X.cygport all

This will create:
  /usr/src/hffzip-1.01-X-src.tar.bz2
  /usr/src/hffzip-1.01-X.tar.bz2

-------------------------------------------

Files included in the binary package:

(hffzip)
  /usr/bin/hffzip.exe
  /usr/share/doc/Cygwin/hffzip.README
  /usr/share/doc/hffzip/COPYING
  /usr/share/doc/hffzip/COPYRIGHT

------------------

Port Notes:

----- version 1.01-1bl1 -----
Initial release for Cygwin-1.7 by fd0 <http://d.hatena.ne.jp/fd0>

