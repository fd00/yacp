--- origsrc/hiredis/CMakeLists.txt	2020-01-28 18:51:59.000000000 +0900
+++ src/hiredis/CMakeLists.txt	2020-01-28 18:58:12.430741500 +0900
@@ -34,7 +34,7 @@ ADD_LIBRARY(hiredis SHARED
 
 SET_TARGET_PROPERTIES(hiredis
     PROPERTIES
-    VERSION "${HIREDIS_SONAME}")
+    SOVERSION "${HIREDIS_SONAME}")
 IF(WIN32 OR MINGW)
     TARGET_LINK_LIBRARIES(hiredis PRIVATE ws2_32)
 ENDIF()
@@ -43,7 +43,8 @@ TARGET_INCLUDE_DIRECTORIES(hiredis PUBLI
 CONFIGURE_FILE(hiredis.pc.in hiredis.pc @ONLY)
 
 INSTALL(TARGETS hiredis
-    DESTINATION "${CMAKE_INSTALL_LIBDIR}")
+    RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}"
+    ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}")
 
 INSTALL(FILES hiredis.h read.h sds.h async.h
     DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}/hiredis)
