HOMEPAGE="http://${PN}.org/"
SRC_URI="http://eddylab.org/software/${PN}/${P}.tar.gz"

CATEGORY="Science"
SUMMARY="Biosequence analysis using profile hidden Markov models"
DESCRIPTION="HMMER is used for searching sequence databases for sequence homologs, and for
making sequence alignments. It implements methods using probabilistic models
called profile hidden Markov models (profile HMMs)."

src_compile()
{
	cd ${B}
	lndirs
	cygautoreconf
	cygconf
	cygmake
}
