category: Net
requires: cygwin libgcc1 libopenssl100
sdesc: "Measure HTTP latency"
ldesc: "http_ping is like the regular ping command, except that it sends HTTP
requests instead of ICMP echo requests."
