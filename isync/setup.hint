category: Mail
requires: cygwin libdb5.2 libopenssl098
sdesc: "Tool to synchronize IMAP4 and Maildir mailboxes"
ldesc: "isync is a command line application which synchronizes mailboxes;
currently Maildir and IMAP4 mailboxes are supported.  New messages, message
deletions and flag changes can be propagated both ways.  isync is suitable for
use in IMAP-disconnected mode."
