--- origsrc/janet-1.11.0/src/core/os.c	2020-07-19 06:21:01.000000000 +0900
+++ src/janet-1.11.0/src/core/os.c	2020-07-23 22:38:34.981457900 +0900
@@ -603,7 +603,7 @@ static Janet os_cryptorand(int32_t argc,
         n -= nread;
     }
     RETRY_EINTR(rc, close(randfd));
-#elif defined(JANET_BSD) || defined(MAC_OS_X_VERSION_10_7)
+#elif defined(JANET_BSD) || defined(MAC_OS_X_VERSION_10_7) || defined(__CYGWIN__)
     (void) genericerr;
     arc4random_buf(buffer->data + offset, n);
 #else
