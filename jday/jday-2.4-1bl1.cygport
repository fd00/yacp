DESCRIPTION="Simple command to convert calendar dates to julian dates"
HOMEPAGE="http://${PN}.sf.net/"
SRC_URI="mirror://sourceforge/${PN}/${P}.tar.gz"

PKG_NAMES="
	${PN}
	lib${PN}2
	lib${PN}-devel
"
jday_CONTENTS="
	usr/bin/*.exe
	usr/share
"
libjday2_CONTENTS="
	usr/bin/*.dll
"
libjday_devel_CONTENTS="
	usr/include
	usr/lib
"
