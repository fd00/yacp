HOMEPAGE="https://github.com/keenerd/${PN}"
SRC_URI="https://github.com/keenerd/${PN}/archive/${PV}.tar.gz"

CATEGORY="Utils"
SUMMARY="JSON parser designed for maximum convenience within the shell"
DESCRIPTION="Jshon is a JSON parser designed for maximum convenience within the shell."

src_compile()
{
	cd ${B}
	lndirs
	cygmake
}
