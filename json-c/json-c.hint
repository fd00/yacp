category: Libs
requires: cygwin
sdesc: "JSON implementation in C (licensing & readmes)"
ldesc: "JSON-C implements a reference counting object model that allows you to
easily construct JSON objects in C, output them as JSON formatted strings and
parse JSON formatted strings back into the C representation of JSON objects."
