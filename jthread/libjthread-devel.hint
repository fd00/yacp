category: Devel Libs
requires: cygwin libjthread1.2_1
external-source: jthread
sdesc: "JThread provides classes to make use of threads easy on different platforms (development)"
ldesc: "The JThread package provides some classes to make use of threads easy on
different platforms. The classes are actually rather simple wrappers around
existing thread implementations."
