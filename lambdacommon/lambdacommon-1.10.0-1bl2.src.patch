--- origsrc/lambdacommon/CMakeLists.txt	2026-05-21 08:34:13.000000000 +0900
+++ src/lambdacommon/CMakeLists.txt	2026-05-21 08:36:03.744544700 +0900
@@ -71,6 +71,9 @@ GENERATE_EXPORT_HEADER(lambdacommon
 # Generate the version header and include it.
 generate_version_header("LAMBDACOMMON" "lambdacommon" ${LAMBDACOMMON_VERSION_MAJOR} ${LAMBDACOMMON_VERSION_MINOR} ${LAMBDACOMMON_VERSION_PATCH} "${LAMBDACOMMON_VERSION_TYPE}")
 target_include_directories(lambdacommon PUBLIC ${CMAKE_BINARY_DIR}/exports)
+if (DEFINED SOVERSION)
+    set_target_properties(lambdacommon PROPERTIES SOVERSION ${SOVERSION})
+endif ()
 if (LAMBDACOMMON_BUILD_STATIC)
     target_compile_definitions(lambdacommon_static PUBLIC -DLAMBDACOMMON_SHARED_EXPORTS_BUILT_AS_STATIC)
     target_include_directories(lambdacommon_static PUBLIC ${CMAKE_BINARY_DIR}/exports)
--- origsrc/lambdacommon/c_wrapper/CMakeLists.txt	2026-05-21 08:34:13.000000000 +0900
+++ src/lambdacommon/c_wrapper/CMakeLists.txt	2026-05-21 08:36:26.541395300 +0900
@@ -69,6 +69,10 @@ target_link_libraries(clambdacommon lamb
 target_include_directories(clambdacommon PUBLIC "include")
 target_include_directories(clambdacommon PUBLIC "${CMAKE_BINARY_DIR}/c_wrapper/exports")
 
+if (DEFINED SOVERSION)
+    set_target_properties(clambdacommon PROPERTIES SOVERSION ${SOVERSION})
+endif ()
+
 # Install if the option is on.
 if (LAMBDACOMMON_INSTALL)
     foreach (LOOP_HEADER ${C_HEADERS_FILES})
--- origsrc/lambdacommon/include/lambdacommon/path.h	2026-05-21 08:34:13.000000000 +0900
+++ src/lambdacommon/include/lambdacommon/path.h	2026-05-21 09:07:34.963703600 +0900
@@ -18,6 +18,8 @@
 #  pragma warning(disable:4251)
 #endif
 
+#include <utility>
+
 namespace lambdacommon
 {
     using namespace std::rel_ops;
