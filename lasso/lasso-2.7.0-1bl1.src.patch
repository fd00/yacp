--- origsrc/lasso-2.7.0/bindings/python/Makefile.am	2021-05-18 05:00:09.051310300 +0900
+++ src/lasso-2.7.0/bindings/python/Makefile.am	2021-07-27 09:51:07.707159600 +0900
@@ -15,7 +15,7 @@ python_LTLIBRARIES = _lasso.la
 
 nodist__lasso_la_SOURCES = _lasso.c
 _lasso_la_CFLAGS = -fno-strict-aliasing $(LASSO_CORE_CFLAGS) $(PY_CFLAGS) $(AM_CFLAGS)
-_lasso_la_LIBADD = $(top_builddir)/lasso/liblasso.la $(LASSO_LIBS)
+_lasso_la_LIBADD = $(top_builddir)/lasso/liblasso.la $(LASSO_LIBS) -lpython$(PYTHON_VERSION)
 if DARWIN
 _lasso_la_LDFLAGS = -no-undefined -module -avoid-version -Wl,-F. -Wl,-F. -bundle -framework Python 
 else
File origsrc/lasso-2.7.0/lasso/lasso_config.h is a regular file while file src/lasso-2.7.0/lasso/lasso_config.h is a symbolic link
--- origsrc/lasso-2.7.0/tests/Makefile.am	2020-12-17 18:22:08.934896700 +0900
+++ src/lasso-2.7.0/tests/Makefile.am	2021-07-27 06:55:55.041002600 +0900
@@ -26,19 +26,16 @@ tests_LDADD = \
 	$(top_builddir)/lasso/liblasso.la \
 	$(LASSO_LIBS) \
 	$(CHECK_LIBS)
-tests_LDFLAGS = -rpath `cd $(top_builddir)/lasso/.libs/; pwd` -static
 
 tests2_SOURCES = tests2.c
 tests2_LDADD = \
 	$(top_builddir)/lasso/liblasso.la \
 	$(LASSO_LIBS) \
 	$(CHECK_LIBS)
-tests2_LDFLAGS = -rpath `cd $(top_builddir)/lasso/.libs/; pwd`
 tests2_CFLAGS = $(AM_CFLAGS) -DSRCDIR=\"$(srcdir)\"
 
 perfs_SOURCES = perfs.c
 perfs_LDADD = $(top_builddir)/lasso/liblasso.la $(LASSO_LIBS)
-perfs_LDFLAGS = -rpath `cd $(top_builddir)/lasso/.libs/; pwd`
 
 .PHONY: valgrind
 
File origsrc/lasso-2.7.0/win32/lasso.rc is a regular file while file src/lasso-2.7.0/win32/lasso.rc is a symbolic link
File origsrc/lasso-2.7.0/win32/msvc/lasso_config.h is a regular file while file src/lasso-2.7.0/win32/msvc/lasso_config.h is a symbolic link
File origsrc/lasso-2.7.0/win32/nsis/jlasso-lite.nsi is a regular file while file src/lasso-2.7.0/win32/nsis/jlasso-lite.nsi is a symbolic link
File origsrc/lasso-2.7.0/win32/nsis/lasso-deps.nsi is a regular file while file src/lasso-2.7.0/win32/nsis/lasso-deps.nsi is a symbolic link
File origsrc/lasso-2.7.0/win32/nsis/lasso-full.nsi is a regular file while file src/lasso-2.7.0/win32/nsis/lasso-full.nsi is a symbolic link
File origsrc/lasso-2.7.0/win32/nsis/lasso-lite.nsi is a regular file while file src/lasso-2.7.0/win32/nsis/lasso-lite.nsi is a symbolic link
