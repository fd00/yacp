--- origsrc/laszip-src-3.4.1/cmake/unix_compiler_options.cmake	2019-04-13 04:13:25.000000000 +0900
+++ src/laszip-src-3.4.1/cmake/unix_compiler_options.cmake	2019-04-23 08:36:34.979486000 +0900
@@ -2,10 +2,12 @@ set(LASZIP_COMMON_CXX_FLAGS "-Wextra -Wa
 )
 
 if (${CMAKE_CXX_COMPILER_ID} MATCHES "GNU")
-    if (${CMAKE_CXX_COMPILER_VERSION} VERSION_LESS 4.7)
-       set(CXX_STANDARD "-std=c++0x")
-    else()
-       set(CXX_STANDARD "-std=c++11")
+    if (NOT CYGWIN)
+        if (${CMAKE_CXX_COMPILER_VERSION} VERSION_LESS 4.7)
+            set(CXX_STANDARD "-std=c++0x")
+        else()
+            set(CXX_STANDARD "-std=c++11")
+        endif()
     endif()
     if (${CMAKE_CXX_COMPILER_VERSION} VERSION_GREATER 4.6)
         set(LASZIP_NO_AS_NEEDED_START "-Wl,--no-as-needed")
