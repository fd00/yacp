--- origsrc/libbitcoin-database-3.8.0/Makefile.am	2023-08-18 10:25:00.000000000 +0900
+++ src/libbitcoin-database-3.8.0/Makefile.am	2025-08-28 00:22:10.548932600 +0900
@@ -57,6 +57,7 @@ src_libbitcoin_database_la_SOURCES = \
     src/primitives/slab_manager.cpp \
     src/result/block_result.cpp \
     src/result/transaction_result.cpp
+src_libbitcoin_database_la_LDFLAGS = -no-undefined
 
 # local: test/libbitcoin-database-test
 #------------------------------------------------------------------------------
--- origsrc/libbitcoin-database-3.8.0/configure.ac	2023-08-18 10:25:00.000000000 +0900
+++ src/libbitcoin-database-3.8.0/configure.ac	2025-08-28 00:22:46.757414100 +0900
@@ -234,17 +234,17 @@ AS_CASE([${CC}], [*],
 
 # Check dependencies.
 #==============================================================================
-# Require Boost of at least version 1.72.0 and output ${boost_CPPFLAGS/LDFLAGS}.
+# Require Boost of at least version 1.66.0 and output ${boost_CPPFLAGS/LDFLAGS}.
 #------------------------------------------------------------------------------
 AS_CASE([${CC}], [*],
-    [AX_BOOST_BASE([1.72.0],
+    [AX_BOOST_BASE([1.66.0],
         [AC_SUBST([boost_CPPFLAGS], [${BOOST_CPPFLAGS}])
          AC_SUBST([boost_ISYS_CPPFLAGS], [`echo ${BOOST_CPPFLAGS} | $SED s/^-I/-isystem/g | $SED s/' -I'/' -isystem'/g`])
          AC_SUBST([boost_LDFLAGS], [${BOOST_LDFLAGS}])
          AC_MSG_NOTICE([boost_CPPFLAGS : ${boost_CPPFLAGS}])
          AC_MSG_NOTICE([boost_ISYS_CPPFLAGS : ${boost_ISYS_CPPFLAGS}])
          AC_MSG_NOTICE([boost_LDFLAGS : ${boost_LDFLAGS}])],
-        [AC_MSG_ERROR([Boost 1.72.0 or later is required but was not found.])])])
+        [AC_MSG_ERROR([Boost 1.66.0 or later is required but was not found.])])])
 
 AS_CASE([${enable_isystem}],[yes],
     [AC_SUBST([boost_BUILD_CPPFLAGS], [${boost_ISYS_CPPFLAGS}])],
