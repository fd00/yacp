--- origsrc/libbox0/libbox0/common.h	2025-08-20 19:58:23.000000000 +0900
+++ src/libbox0/libbox0/common.h	2025-08-20 19:58:28.355018700 +0900
@@ -65,6 +65,7 @@ __BEGIN_DECLS
 #define B0_PACKED __attribute__((packed))
 
 /* thanks Bionic */
+#if !defined(__restrict)
 #if defined(__STDC__VERSION__) && __STDC_VERSION__ >= 199901L
 # define __restrict	restrict
 #else
@@ -73,6 +74,7 @@ __BEGIN_DECLS
 #  define __restrict /* delete __restrict when not supported */
 # endif
 #endif
+#endif
 
 /* Just for readability */
 #define B0_IS_NULL(ptr) (ptr == NULL)
--- origsrc/libbox0/libbox0/libbox0.pc.in	2025-08-20 19:58:23.000000000 +0900
+++ src/libbox0/libbox0/libbox0.pc.in	2025-08-20 20:14:22.397085400 +0900
@@ -7,8 +7,8 @@ Name: ${PROJECT_NAME}
 Description: ${PROJECT_DESCRIPTION_SHORT}
 URL: ${PROJECT_URL}
 Requires: ${deps}
-Requires.private: ${libs}
-Version: ${libbox0_VERSION}
+Requires.private:
+Version: ${VERSION}
 Libs: -L${libdir} -lbox0
 Libs.private: ${libs}
 Cflags: -I${includedir}
