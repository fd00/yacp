DESCRIPTION="Portable C library for reading and writing BSB format image files"
HOMEPAGE="http://${PN}.sourceforge.net/"
SRC_URI="mirror://sourceforge/${PN}/${P}.tar.gz"

PKG_NAMES="${PN} ${PN}0 ${PN}-devel"
libbsb_CONTENTS="usr/bin/*.exe usr/share"
libbsb0_CONTENTS="usr/bin/*.dll"
libbsb_devel_CONTENTS="usr/include usr/lib"

DIFF_EXCLUDES="testsuite"
