--- origsrc/libclsp-0.1.3/CMakeLists.txt	2020-01-16 05:40:52.000000000 +0900
+++ src/libclsp-0.1.3/CMakeLists.txt	2026-03-26 04:43:27.186847100 +0900
@@ -30,19 +30,18 @@ include(GNUInstallDirs)
 include(FindPkgConfig)
 
 # The library itself
-add_library(${PROJECT_NAME} SHARED)
+add_library(clsp SHARED)
 
 # Version for the library symlinks
-set_target_properties(${PROJECT_NAME}
+set_target_properties(clsp
 	PROPERTIES
 		VERSION ${PROJECT_VERSION}
 		SOVERSION ${PROJECT_VERSION_MAJOR}
-		PREFIX ""
 		CXX_STANDARD 17
 )
 
 # Headers
-target_include_directories(${PROJECT_NAME}
+target_include_directories(clsp
 	PRIVATE ${PROJECT_SOURCE_DIR}/include
 )
 
@@ -59,7 +58,7 @@ configure_file(libclsp.pc.in
 )
 install(FILES
 	${CMAKE_BINARY_DIR}/libclsp.pc
-	DESTINATION ${CMAKE_INSTALL_DATAROOTDIR}/pkgconfig
+	DESTINATION ${CMAKE_INSTALL_LIBDIR}/pkgconfig
 )
 
 # Libraries
@@ -67,21 +66,21 @@ pkg_check_modules(RAPIDJSON REQUIRED Rap
 include(FindBoost)
 
 # Header path
-target_include_directories(${PROJECT_NAME}
+target_include_directories(clsp
 	PUBLIC
 		${RAPIDJSON_INCLUDE_DIRS}
 		${Boost_INCLUDE_DIRS}
 )
 
 # Linking
-target_link_libraries(${PROJECT_NAME}
+target_link_libraries(clsp
 	PUBLIC
 		${RAPIDJSON_LIBRARIES}
 		${Boost_LIBRARIES}
 )
 
 # Other flags
-target_compile_definitions(${PROJECT_NAME}
+target_compile_definitions(clsp
 	PUBLIC
 		${RAPIDJSON_CFLAGS_OTHER}
 )
@@ -92,6 +91,6 @@ if(NOT DEFINED ENV{CXXFLAGS})
 endif()
 
 
-install(TARGETS ${PROJECT_NAME}
+install(TARGETS clsp
 	LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
 )
--- origsrc/libclsp-0.1.3/src/types/CMakeLists.txt	2020-01-16 05:40:52.000000000 +0900
+++ src/libclsp-0.1.3/src/types/CMakeLists.txt	2026-03-26 04:43:52.141672800 +0900
@@ -14,7 +14,7 @@
 # You should have received a copy of the GNU General Public License
 # along with libclsp.  If not, see <http://www.gnu.org/licenses/>.
 
-target_sources(${PROJECT_NAME}
+target_sources(clsp
 	PRIVATE
 		applyWorkspaceEdit.cpp
 		cancelParams.cpp
