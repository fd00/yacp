--- origsrc/libdivecomputer-0.9.0/src/context-private.h	2025-07-01 05:24:23.000000000 +0900
+++ src/libdivecomputer-0.9.0/src/context-private.h	2026-01-17 17:03:04.593693700 +0900
@@ -42,6 +42,10 @@ extern "C" {
 #define FUNCTION __FUNCTION__
 #endif
 
+#if defined(ERROR)
+#undef ERROR
+#endif
+
 #ifdef ENABLE_LOGGING
 #define HEXDUMP(context, loglevel, prefix, data, size) dc_context_hexdump (context, loglevel, __FILE__, __LINE__, FUNCTION, prefix, data, size)
 #define SYSERROR(context, errcode) dc_context_syserror (context, DC_LOGLEVEL_ERROR, __FILE__, __LINE__, FUNCTION, errcode)
--- origsrc/libdivecomputer-0.9.0/src/oceanic_common.h	2025-07-01 05:24:23.000000000 +0900
+++ src/libdivecomputer-0.9.0/src/oceanic_common.h	2026-01-17 17:03:04.598775500 +0900
@@ -131,6 +131,10 @@ extern "C" {
 #define I330R         0x4744
 #define I330R_C       0x474D
 
+#if defined(PAGESIZE)
+#undef PAGESIZE
+#endif
+
 #define PAGESIZE 0x10
 #define FPMAXSIZE 0x200
 
--- origsrc/libdivecomputer-0.9.0/src/serial_posix.c	2025-07-01 05:24:23.000000000 +0900
+++ src/libdivecomputer-0.9.0/src/serial_posix.c	2026-01-17 17:03:04.600788900 +0900
@@ -298,6 +298,7 @@ dc_serial_open (dc_iostream_t **out, dc_
 		goto error_timer_free;
 	}
 
+#if defined(TIOCEXCL)
 #ifndef ENABLE_PTY
 	// Enable exclusive access mode.
 	if (ioctl (device->fd, TIOCEXCL, NULL) != 0) {
@@ -307,6 +308,7 @@ dc_serial_open (dc_iostream_t **out, dc_
 		goto error_close;
 	}
 #endif
+#endif
 
 	// Retrieve the current terminal attributes, to
 	// be able to restore them when closing the device.
@@ -345,6 +347,7 @@ dc_serial_close (dc_iostream_t *abstract
 		dc_status_set_error(&status, syserror (errcode));
 	}
 
+#if defined(TIOCNXCL)
 #ifndef ENABLE_PTY
 	// Disable exclusive access mode.
 	if (ioctl (device->fd, TIOCNXCL, NULL)) {
@@ -353,6 +356,7 @@ dc_serial_close (dc_iostream_t *abstract
 		dc_status_set_error(&status, syserror (errcode));
 	}
 #endif
+#endif
 
 	// Close the device.
 	if (close (device->fd) != 0) {
