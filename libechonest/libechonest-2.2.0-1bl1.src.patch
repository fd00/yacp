--- origsrc/libechonest-2.2.0/src/CMakeLists.txt	2014-02-14 23:55:03.000000000 +0900
+++ src/libechonest-2.2.0/src/CMakeLists.txt	2014-07-04 08:21:16.691916600 +0900
@@ -41,11 +41,11 @@ set( LIBECHONEST_H
 QT_WRAP_CPP( ${LIBECHONEST_H} )
 
 SET( OS_SPECIFIC_LINK_LIBRARIES "" )
-IF( APPLE OR MINGW )
+IF( APPLE OR MINGW OR CYGWIN )
   SET( OS_SPECIFIC_LINK_LIBRARIES ${QJSON_LIBRARIES} )
-ELSE( APPLE OR MINGW )
+ELSE( APPLE OR MINGW OR CYGWIN )
   SET( OS_SPECIFIC_LINK_LIBRARIES ${QJSON_LDFLAGS} )
-ENDIF( APPLE OR MINGW )
+ENDIF( APPLE OR MINGW OR CYGWIN )
 
 
 add_library( echonest SHARED ${LIBECHONEST_SRC} )
