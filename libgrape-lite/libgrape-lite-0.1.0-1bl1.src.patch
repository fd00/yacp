--- origsrc/libgrape-lite-0.1.0/CMakeLists.txt	2021-10-29 12:57:19.000000000 +0900
+++ src/libgrape-lite-0.1.0/CMakeLists.txt	2022-05-08 15:26:06.345798200 +0900
@@ -59,6 +59,8 @@ set(CMAKE_INSTALL_RPATH_USE_LINK_PATH TR
 set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11 -Wall")
 if (APPLE)
     set(CMAKE_MACOSX_RPATH ON)
+elseif (CYGWIN)
+    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fopenmp -Wl,-rpath,$ORIGIN")
 else ()
     set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fopenmp -Werror -Wl,-rpath,$ORIGIN")
 endif ()
@@ -130,6 +132,10 @@ file(GLOB_RECURSE CORE_SRC_FILES "grape/
 add_library(grape-lite ${CORE_SRC_FILES})
 install_libgrapelite_target(grape-lite)
 
+if (CYGWIN)
+    set_target_properties(grape-lite PROPERTIES SOVERSION ${LIBGRAPELITE_MAJOR_VERSION})
+endif()
+
 target_link_libraries(grape-lite ${MPI_CXX_LIBRARIES}
         ${CMAKE_THREAD_LIBS_INIT}
         ${GLOG_LIBRARIES})
@@ -254,3 +260,11 @@ if(BUILD_LIBGRAPELITE_DOCS)
             VERBATIM)
 endif()
 
+if (CYGWIN)
+  set(prefix ${CMAKE_INSTALL_PREFIX})
+  set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+  set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+  set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+  configure_file(${CMAKE_SOURCE_DIR}/libgrape-lite.pc.in ${CMAKE_BINARY_DIR}/libgrape-lite.pc @ONLY)
+  install(FILES ${CMAKE_BINARY_DIR}/libgrape-lite.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
+endif()
--- origsrc/libgrape-lite-0.1.0/libgrape-lite.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/libgrape-lite-0.1.0/libgrape-lite.pc.in	2022-05-08 15:28:14.330685800 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: libgrape-lite
+Description: C++ library for parallel graph processing
+Version: @LIBGRAPELITE_VERSION@
+Libs: -L${libdir} -lgrape-lite
+Cflags: -I${includedir}
