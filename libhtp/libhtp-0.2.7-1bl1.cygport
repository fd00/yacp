GIT_URI="git://github.com/ironbee/${PN}.git"
GIT_TAG="${PV}"
inherit git

DESCRIPTION="Security-aware parser for the HTTP protocol"
HOMEPAGE="https://github.com/ironbee/${PN}"

CYGCONF_ARGS="--disable-static"

src_test()
{
	cd ${B}
	cygmake check
	cd test
	ln -s ${S}/test/files .
	./main.exe
	rm -f files
}

PKG_NAMES="
	${PN}
	${PN}0.2_1
	${PN}-devel"
libhtp_CONTENTS="
	usr/share
"
libhtp0_2_1_CONTENTS="
	usr/bin
"
libhtp_devel_CONTENTS="
	usr/include
	usr/lib
"
