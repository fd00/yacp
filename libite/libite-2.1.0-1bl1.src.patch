diff -buprN -x CYGWIN-PATCHES -x aclocal.m4~ -x aclocal.m4t -x autom4te.cache -x config.cache -x config.guess -x config.log -x config.status -x config.sub -x '*SlackBuild*' -x '*.egg-info' -x '*.dll' -x '*.exe' -x '*.o' -x '*.gch' -x '*.gem' -x .gitignore -x '*.jpg' -x '*.png' -x '*.qm' -x '*.svgz' -x '*.tif' -x '*.class' -x '*.jlc' -x '*.pyc' -x '*.pyo' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*.whl' -x '*~' -x '*.stackdump' -x x86_64-pc-cygwin -x aclocal.m4 -x configure -x config.h -x '*.h.in' -x COPYING -x INSTALL -x ansi2knr.c -x ar-lib -x config-ml.in -x elisp-comp -x mdate-sh -x mkinstalldirs -x py-compile -x symlink-tree -x tap-driver.sh -x test-driver -x texinfo.tex -x ylwrap -x Makefile.in -x GNUmakefile.in -x makefile.in -x libtool.m4 -x ltoptions.m4 -x ltsugar.m4 -x ltversion.m4 -x lt~obsolete.m4 -x ltmain.sh -x compile -x depcomp -x install-sh -x missing -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x mate-doc-utils.make -x mate-doc-utils.m4 origsrc/libite-2.1.0/src/Makefile.am src/libite-2.1.0/src/Makefile.am
--- origsrc/libite-2.1.0/src/Makefile.am	2019-08-27 04:59:57.000000000 +0900
+++ src/libite-2.1.0/src/Makefile.am	2020-01-22 11:20:46.921188700 +0900
@@ -2,7 +2,7 @@ lib_LTLIBRARIES       = libite.la
 
 libite_la_CPPFLAGS    = -D_GNU_SOURCE
 libite_la_CFLAGS      = -W -Wall -Wextra
-libite_la_LDFLAGS     = $(AM_LDFLAGS) -version-info 5:1:0
+libite_la_LDFLAGS     = $(AM_LDFLAGS) -no-undefined -version-info 5:1:0
 libite_la_SOURCES     = chomp.c		copyfile.c			\
 			conio.c		conio.h				\
 			dir.c						\
diff -buprN -x CYGWIN-PATCHES -x aclocal.m4~ -x aclocal.m4t -x autom4te.cache -x config.cache -x config.guess -x config.log -x config.status -x config.sub -x '*SlackBuild*' -x '*.egg-info' -x '*.dll' -x '*.exe' -x '*.o' -x '*.gch' -x '*.gem' -x .gitignore -x '*.jpg' -x '*.png' -x '*.qm' -x '*.svgz' -x '*.tif' -x '*.class' -x '*.jlc' -x '*.pyc' -x '*.pyo' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*.whl' -x '*~' -x '*.stackdump' -x x86_64-pc-cygwin -x aclocal.m4 -x configure -x config.h -x '*.h.in' -x COPYING -x INSTALL -x ansi2knr.c -x ar-lib -x config-ml.in -x elisp-comp -x mdate-sh -x mkinstalldirs -x py-compile -x symlink-tree -x tap-driver.sh -x test-driver -x texinfo.tex -x ylwrap -x Makefile.in -x GNUmakefile.in -x makefile.in -x libtool.m4 -x ltoptions.m4 -x ltsugar.m4 -x ltversion.m4 -x lt~obsolete.m4 -x ltmain.sh -x compile -x depcomp -x install-sh -x missing -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x mate-doc-utils.make -x mate-doc-utils.m4 origsrc/libite-2.1.0/src/ifconfig.c src/libite-2.1.0/src/ifconfig.c
--- origsrc/libite-2.1.0/src/ifconfig.c	2019-08-27 04:59:57.000000000 +0900
+++ src/libite-2.1.0/src/ifconfig.c	2020-01-22 13:30:16.703020900 +0900
@@ -59,13 +59,13 @@ int ifconfig(const char *ifname, const c
 	if (up) {
 		if (addr) {
 			if (inet_pton(AF_INET, addr, &sin->sin_addr) == 1)
-				ret = ioctl(sd, SIOCSIFADDR, &ifr);
+				ret = ioctl(sd, SIOCGIFADDR, &ifr);
 		}
 
 		/* Non-zero IP address */
 		if (mask && addr && strcmp(addr, "0.0.0.0")) {
 			if (inet_pton(AF_INET, mask, &sin->sin_addr) == -1)
-				ret = ioctl(sd, SIOCSIFNETMASK, &ifr);
+				ret = ioctl(sd, SIOCGIFNETMASK, &ifr);
 		}
 	}
 
@@ -75,7 +75,7 @@ int ifconfig(const char *ifname, const c
 		else
 			ifr.ifr_flags &= ~IFF_UP;
 
-		ret = ioctl(sd, SIOCSIFFLAGS, &ifr);
+		ret = ioctl(sd, SIOCGIFFLAGS, &ifr);
 	}
 
 	close(sd);
