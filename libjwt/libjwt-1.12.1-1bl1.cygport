HOMEPAGE="https://github.com/benmcollins/${PN}"
SRC_URI="https://github.com/benmcollins/${PN}/archive/v${PV}.tar.gz"

CATEGORY="Libs"
SUMMARY="JSON Web Tokens C Library"
DESCRIPTION="${SUMMARY}"

PKG_NAMES="
	${PN}
	${PN}1
	${PN}-devel
"
libjwt_CONTENTS="
	usr/bin/*.exe
	usr/share
"
libjwt1_CONTENTS="
	usr/bin/*.dll
"
libjwt_devel_CONTENTS="
	usr/include
	usr/lib
"
libjwt_SUMMARY="${SUMMARY} (utilities)"
libjwt1_SUMMARY="${SUMMARY} (runtime)"
libjwt_devel_SUMMARY="${SUMMARY} (development)"
