--- origsrc/libndef-libndef-1.2.0/libndef/libndef.pro	2013-12-20 20:02:17.000000000 +0900
+++ src/libndef-libndef-1.2.0/libndef/libndef.pro	2024-10-26 11:01:42.319879900 +0900
@@ -52,7 +52,7 @@ unix: {
 
     # install pkg-config file (libndef.pc)
     CONFIG += create_pc create_prl
-    QMAKE_PKGCONFIG_REQUIRES = QtCore
+    # QMAKE_PKGCONFIG_REQUIRES = QtCore
     QMAKE_PKGCONFIG_LIBDIR = $$target.path
     QMAKE_PKGCONFIG_INCDIR = $$incfiles.path
     QMAKE_PKGCONFIG_DESTDIR = pkgconfig
--- origsrc/libndef-libndef-1.2.0/tools/ndef-decode.cpp	2013-12-20 20:02:17.000000000 +0900
+++ src/libndef-libndef-1.2.0/tools/ndef-decode.cpp	2024-10-26 09:07:28.830743200 +0900
@@ -18,6 +18,7 @@
  */
 
 #include <QtCore/QCoreApplication>
+#include <QDataStream>
 #include <QDebug>
 #include <QStringList>
 #include <QFile>
--- origsrc/libndef-libndef-1.2.0/tools/ndef-decode.pro	2013-12-20 20:02:17.000000000 +0900
+++ src/libndef-libndef-1.2.0/tools/ndef-decode.pro	2024-10-26 09:07:28.830743200 +0900
@@ -36,11 +36,11 @@ win32: {
 unix: {
     # Link to the library generated by the project.  Could use variables or
     # something here to make it more bulletproof
-    LIBS += ../libndef/libndef.so
+    LIBS += ../libndef/libndef.dll.a
 
     # Specify that we depend on the library (which, logically would be implicit from
     # the fact that we are linking to it)
-    PRE_TARGETDEPS += ../libndef/libndef.so
+    PRE_TARGETDEPS += ../libndef/libndef.dll.a
 }
 
 SOURCES += ndef-decode.cpp
@@ -53,4 +53,3 @@ unix: {
     target.path = $$PREFIX/bin
     INSTALLS += target
 }
-
--- origsrc/libndef-libndef-1.2.0/tools/ndef-encode.pro	2013-12-20 20:02:17.000000000 +0900
+++ src/libndef-libndef-1.2.0/tools/ndef-encode.pro	2024-10-26 09:07:28.846369200 +0900
@@ -36,11 +36,11 @@ win32: {
 unix: {
     # Link to the library generated by the project.  Could use variables or
     # something here to make it more bulletproof
-    LIBS += ../libndef/libndef.so
+    LIBS += ../libndef/libndef.dll.a
 
     # Specify that we depend on the library (which, logically would be implicit from
     # the fact that we are linking to it)
-    PRE_TARGETDEPS += ../libndef/libndef.so
+    PRE_TARGETDEPS += ../libndef/libndef.dll.a
 }
 
 SOURCES += ndef-encode.cpp
@@ -53,4 +53,3 @@ unix: {
     target.path = $$PREFIX/bin
     INSTALLS += target
 }
-
