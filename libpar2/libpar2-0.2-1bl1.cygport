DESCRIPTION="Library for performing comman tasks related to PAR recovery sets"
HOMEPAGE="http://parchive.sourceforge.net/"
SRC_URI="mirror://sourceforge/parchive/${P}.tar.gz"

CYGPORT_USE_UNSTABLE_API=1
src_patch_hook()
{
	cd ${origsrcdir}/${SRC_DIR}
	rm -f aclocal.m4 configure
	find . -name Makefile.in | xargs rm -f
}

DOCS="PORTING ROADMAP"

PKG_NAMES="${PN} ${PN}_0 ${PN}-devel"
libpar2_CONTENTS="usr/share"
libpar2_0_CONTENTS="usr/bin"
libpar2_devel_CONTENTS="usr/include usr/lib"
