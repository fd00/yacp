--- origsrc/libredblack-1.3/Makefile.am	2003-10-23 13:40:19.000000000 +0900
+++ src/libredblack-1.3/Makefile.am	2026-03-30 13:35:59.524313800 +0900
@@ -1,7 +1,7 @@
 lib_LTLIBRARIES=libredblack.la
 include_HEADERS=redblack.h
 libredblack_la_SOURCES=redblack.c
-libredblack_la_LDFLAGS= -version-info @REDBLACK_LIB_CURRENT@:@REDBLACK_LIB_REVISION@:@REDBLACK_LIB_AGE@
+libredblack_la_LDFLAGS= -version-info @REDBLACK_LIB_CURRENT@:@REDBLACK_LIB_REVISION@:@REDBLACK_LIB_AGE@ -no-undefined
 man_MANS=rbdelete.3 rbdestroy.3 rbfind.3 rbinit.3 rbsearch.3 rblookup.3 \
 	rbwalk.3 rbopenlist.3 rbreadlist.3 rbcloselist.3 rbgen.1
 EXTRA_DIST=autogen.sh @PACKAGE@.spec @PACKAGE@.spec.in $(man_MANS) example4.rb
@@ -21,7 +21,7 @@ example3_LDADD=libredblack.la
 nodist_example4_SOURCES=example4.c
 example4_LDADD=
 CLEANFILES=example4.c
-CFLAGS=-Wall
+TESTS = example1 example2 example3 example4
 
 if USE_RBGEN
 SUFFIXES = .rb
@@ -56,9 +56,14 @@ example4.c: example4.rb
 	## Create a dummy example4.c in case rbgen isn't present
 	echo 'int main() { return(0); }' > example4.c
 if USE_RBGEN
-	./rbgen $<
+	sed -e 's:"/share/redblack"]:"/share/redblack", "../src/libredblack-1.3"]:' rbgen > rbgen.py
+	chmod u+x rbgen.py
+	./rbgen.py $<
 endif
 
 writelock:
 	chmod a-w $(HEADERS) $(SOURCES) $(MANS) \
 		AUTHORS NEWS README COPYING INSTALL
+
+pkgconfigdir = $(libdir)/pkgconfig
+pkgconfig_DATA = $(PACKAGE).pc
--- origsrc/libredblack-1.3/configure.in	2003-10-23 13:18:47.000000000 +0900
+++ src/libredblack-1.3/configure.in	2026-03-30 13:12:57.023490700 +0900
@@ -76,4 +76,4 @@ AC_CHECK_FUNCS(strdup)
 
 AM_CONDITIONAL(USE_RBGEN, test "$with_rbgen" = "yes")
 
-AC_OUTPUT(Makefile libredblack.spec rbgen, [chmod a+x rbgen])
+AC_OUTPUT(libredblack.pc Makefile libredblack.spec rbgen, [chmod a+x rbgen])
--- origsrc/libredblack-1.3/libredblack.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/libredblack-1.3/libredblack.pc.in	2026-03-30 13:12:57.025486200 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: libredblack
+Description: RedBlack Balanced Tree Searching and Sorting Library
+Version: @VERSION@
+Libs: -L${libdir} -lredblack
+Cflags: -I${includedir}
--- origsrc/libredblack-1.3/rbgen.in	2003-10-24 10:31:21.000000000 +0900
+++ src/libredblack-1.3/rbgen.in	2026-03-30 13:26:11.855454700 +0900
@@ -6,7 +6,7 @@
 #
 # $Log: rbgen.in,v $
 # Revision 1.3  2003/10/24 01:31:21  damo
-# Patches from Eric Raymond: %prefix is implemented.  Various other small
+# Patches from Eric Raymond: %prefix is implemented. Various other small
 # changes avoid stepping on global namespaces and improve the documentation.
 #
 # Revision 1.2  2003/10/23 04:18:47  damo
@@ -21,7 +21,7 @@
 #
 #
 
-import sys, string, os
+import sys, os
 
 fpin = sys.stdin
 lineno = 0
@@ -45,10 +45,11 @@ def include(file, fpout):
 def redblack(filename, lines):
     rb_type = rb_cmp = rb_alloc = rb_free = rb_access = None
     rb_prefix = "rb"
+    rb_omit = []
     sbrk = static = 0
     global lineno
 
-    ext = string.find(filename, ".rb")
+    ext = filename.find(".rb")
     if ext == -1:
         sys.stderr.write("redblack: extension on %s doesn't look right.\n" % filename)
         raise SyntaxError
@@ -72,13 +73,13 @@ def redblack(filename, lines):
                 lineno = lineno + 1
                 if not line:
                     break
-                comment = string.find(line, "//")
+                comment = line.find("//")
                 if comment > -1:
                     line = line[:comment] + "\n"
                 if line[0] == "\n":
                     continue
                 elif startswith(line, "%%rbgen"):
-		    fpout.write("#define RB_CUSTOMIZE\n")
+                    fpout.write("#define RB_CUSTOMIZE\n")
                     if not rb_type:
                         error("%type directive is required.")
                     else:
@@ -115,9 +116,14 @@ def redblack(filename, lines):
                     fpout.write(fpin.read())
                     return
                 elif line[0] == "%":
-                    tokens = string.split(line)
-                    if tokens[0] in("%type","%cmp","%alloc","%free","%prefix"):
-                        exec "rb_" + tokens[0][1:] + " = '" + tokens[1] + "'"
+                    tokens = line.split()
+                    if tokens[0] in ("%type", "%cmp", "%alloc", "%free", "%prefix"):
+                        key = tokens[0][1:]
+                        if key == "type": rb_type = tokens[1]
+                        elif key == "cmp": rb_cmp = tokens[1]
+                        elif key == "alloc": rb_alloc = tokens[1]
+                        elif key == "free": rb_free = tokens[1]
+                        elif key == "prefix": rb_prefix = tokens[1]
                     elif tokens[0] == "%access":
                         if tokens[1] in ("inline", "pointer"):
                             rb_access = tokens[1]
@@ -137,7 +143,7 @@ def redblack(filename, lines):
                     elif tokens[0] == "%static":
                         static = 1
                     else:
-                        error("unknown directive %s" % string.split(line)[0])
+                        error("unknown directive %s" % line.split()[0])
                 else:
                     error("garbage on line beginning %s." % line[0])
             else:
@@ -153,9 +159,9 @@ if __name__ == "__main__":
     lines = 1;
     skelpath = [".", "@prefix@/share/@PACKAGE@" "/share/redblack"]
     for (switch, val) in options:
-	if (switch == '-l'):
-	    lines = 0
-	elif (switch == '-S'):
+        if (switch == '-l'):
+            lines = 0
+        elif (switch == '-S'):
             skeldir = val
 
     for skeldir in skelpath:
@@ -169,9 +175,9 @@ if __name__ == "__main__":
     for file in arguments:
         try:
             redblack(file, lines)
-            raise SystemExit, 0
+            raise SystemExit(0)
         except SyntaxError:
-            raise SystemExit, 1
+            raise SystemExit(1)
         
 # The following sets edit modes for GNU EMACS
 # Local Variables:
