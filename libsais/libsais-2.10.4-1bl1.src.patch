--- origsrc/libsais-2.10.4/CMakeLists.txt	2025-09-02 12:24:36.000000000 +0900
+++ src/libsais-2.10.4/CMakeLists.txt	2026-01-20 16:01:20.430953800 +0900
@@ -23,17 +23,15 @@ else()
     set(LIBSAIS_LIBRARY_TYPE STATIC)
 endif()
 
-add_library(libsais ${LIBSAIS_LIBRARY_TYPE})
+add_library(sais ${LIBSAIS_LIBRARY_TYPE})
 
-set_target_properties(libsais PROPERTIES
-    PREFIX ""
-    IMPORT_PREFIX ""
+set_target_properties(sais PROPERTIES
     VERSION ${PROJECT_VERSION}
     SOVERSION ${PROJECT_VERSION_MAJOR}
     POSITION_INDEPENDENT_CODE ON
 )
 
-target_sources(libsais PRIVATE
+target_sources(sais PRIVATE
     include/libsais.h
     include/libsais16.h
     include/libsais16x64.h
@@ -49,22 +47,22 @@ if(LIBSAIS_USE_OPENMP)
 endif()
 
 if(LIBSAIS_USE_OPENMP AND OpenMP_C_FOUND)
-    target_compile_definitions(libsais PUBLIC LIBSAIS_OPENMP)
-    target_link_libraries(libsais PUBLIC OpenMP::OpenMP_C)
+    target_compile_definitions(sais PUBLIC LIBSAIS_OPENMP)
+    target_link_libraries(sais PUBLIC OpenMP::OpenMP_C)
 endif()
 
 if(LIBSAIS_BUILD_SHARED_LIB)
-    target_compile_definitions(libsais PUBLIC LIBSAIS_SHARED)
-    target_compile_definitions(libsais PRIVATE LIBSAIS_EXPORTS)
+    target_compile_definitions(sais PUBLIC LIBSAIS_SHARED)
+    target_compile_definitions(sais PRIVATE LIBSAIS_EXPORTS)
 endif()
 
-target_include_directories(libsais PUBLIC
+target_include_directories(sais PUBLIC
     $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
     $<INSTALL_INTERFACE:include>
 )
 
 install(
-    TARGETS libsais
+    TARGETS sais
     EXPORT libsaisTargets
     RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
     LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
@@ -92,4 +90,11 @@ write_basic_package_version_file(
 install(
     FILES "${CMAKE_CURRENT_BINARY_DIR}/libsaisConfigVersion.cmake"
     DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/libsais
-)
\ No newline at end of file
+)
+
+set(prefix ${CMAKE_INSTALL_PREFIX})
+set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+configure_file(${CMAKE_SOURCE_DIR}/libsais.pc.in ${CMAKE_BINARY_DIR}/libsais.pc @ONLY)
+install(FILES ${CMAKE_BINARY_DIR}/libsais.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/libsais-2.10.4/libsais.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/libsais-2.10.4/libsais.pc.in	2026-01-20 16:01:32.218126000 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: libsais
+Description: Library for linear time suffix array
+Version: @PROJECT_VERSION@
+Libs: -L${libdir} -lsais
+Cflags: -I${includedir}
