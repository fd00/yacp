category: Libs
requires: cygwin libcsoap1.1_1
sdesc: "Client/server SOAP library in pure C (utilities)"
ldesc: "csoap is a client/server SOAP library implemented in pure C. It comes
with an embedded HTTP server called nanohttp. The transfered XML structures
are handled by libxml2. csoap comes with the followin features:
  - client/server HTTP engine
  - Attachments via MIME
  - https (SSL) with OpenSSL"
