--- origsrc/libstrophe/Makefile.am	2010-12-23 23:25:17.000000000 +0900
+++ src/libstrophe/Makefile.am	2010-12-23 23:32:50.573305200 +0900
@@ -1,4 +1,4 @@
-ACLOCAL_AMFLAGS=-I m4
+#ACLOCAL_AMFLAGS=-I m4
 AUTOMAKE_OPTIONS = subdir-objects
 
 AM_CFLAGS = -g
@@ -35,6 +35,7 @@ libstrophe_la_SOURCES = src/auth.c src/c
 	src/jid.c src/md5.c src/sasl.c src/sha1.c \
 	src/snprintf.c src/sock.c src/stanza.c \
 	src/tls_openssl.c src/util.c src/*.h
+libstrophe_la_LDFLAGS = -no-undefined
 # ALK: this is not used presently, but causes extra dependency on -lpthread
 # libstrophe_la_SOURCES += src/thread.c
 if PARSER_EXPAT
--- origsrc/libstrophe/configure.ac	2010-12-23 23:25:17.000000000 +0900
+++ src/libstrophe/configure.ac	2010-12-23 23:29:18.047708400 +0900
@@ -1,7 +1,7 @@
 AC_INIT([libstrophe], [trunk], [jack@metajack.im])
 AC_CONFIG_MACRO_DIR([m4])
 AM_INIT_AUTOMAKE
-AC_PROG_LIBTOOL
+LT_INIT(win32-dll)
 
 AC_PROG_CC
 
@@ -72,35 +72,18 @@ echo "Regression tests will not be built
 AM_CONDITIONAL([HAVE_CHECK], [test x$HAVE_CHECK = xyes])
 
 LIBXML2_REQUIRE=
-AC_ARG_WITH([libxml2],
-            [AS_HELP_STRING([--with-libxml2], [use libxml2 for XML parsing])],
-            [with_libxml2=check],
-            [with_libxml2=no])
-
-if test "x$with_libxml2" != xno; then
-  PKG_CHECK_MODULES([libxml2], [libxml-2.0 >= 2.7],
-                    [with_libxml2=yes; LIBXML2_REQUIRE="libxml-2.0"],
-                    [AC_MSG_ERROR([couldn't find libxml2])])
-else
-  AC_CHECK_FUNCS(memmove)
-  AC_C_BIGENDIAN([byteorder=1234], [byteorder=4321], [], [])
-fi
+PKG_CHECK_MODULES([libxml2], [libxml-2.0 >= 2.7],
+                  [with_libxml2=yes; LIBXML2_REQUIRE="libxml-2.0"],
+                  [AC_MSG_ERROR([couldn't find libxml2])])
 AC_SUBST(LIBXML2_REQUIRE)
 
 AC_CHECK_FUNCS(snprintf)
 AC_CHECK_FUNCS(vsnprintf)
 
-if test "x$with_libxml2" = xyes; then
-  with_parser=libxml2
-  PARSER_NAME=libxml2
-  PARSER_CFLAGS=\$\(libxml2_CFLAGS\)
-  PARSER_LIBS=\$\(libxml2_LIBS\)
-else
-  with_parser=builtin
-  PARSER_NAME=expat
-  PARSER_CFLAGS=\$\(builtin_CFLAGS\)
-  PARSER_LIBS=\$\(builtin_LIBS\)
-fi
+with_parser=libxml2
+PARSER_NAME=libxml2
+PARSER_CFLAGS=\$\(libxml2_CFLAGS\)
+PARSER_LIBS=\$\(libxml2_LIBS\)
 
 AC_MSG_NOTICE([libstrophe will use the $with_parser XML parser])
 AC_SEARCH_LIBS([socket], [socket])
