DESCRIPTION="Library for controlling a VISCA(tm) compliant camera"
HOMEPAGE="http://damien.douxchamps.net/${PN}/"
SRC_URI="mirror://sourceforge/${PN}/${P}.tar.gz"

CYGCONF_ARGS="--disable-static"

PKG_NAMES="${PN} ${PN}0 ${PN}-devel"
libvisca_CONTENTS="usr/share"
libvisca0_CONTENTS="usr/bin"
libvisca_devel_CONTENTS="usr/include usr/lib"
