--- origsrc/libvisio2svg-0.5.8/CMakeLists.txt	2025-12-14 22:08:54.000000000 +0900
+++ src/libvisio2svg-0.5.8/CMakeLists.txt	2026-01-22 22:32:43.555785700 +0900
@@ -161,3 +161,10 @@ INSTALL(TARGETS vss2svg-conv vsd2svg-con
 )
 
 INSTALL(DIRECTORY inc/visio2svg DESTINATION ${INCLUDE_INSTALL_DIR})
+
+set(prefix ${CMAKE_INSTALL_PREFIX})
+set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+configure_file(${CMAKE_SOURCE_DIR}/libvisio2svg.pc.in ${CMAKE_BINARY_DIR}/libvisio2svg.pc @ONLY)
+install(FILES ${CMAKE_BINARY_DIR}/libvisio2svg.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/libvisio2svg-0.5.8/libvisio2svg.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/libvisio2svg-0.5.8/libvisio2svg.pc.in	2026-01-22 22:33:01.508846800 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: libvisio2svg
+Description: Library/Tools to convert Microsoft (MS) Visio documents (VSS and VSD) to SVG
+Version: @vss2svg_VERSION@
+Libs: -L${libdir} -lvisio2svg
+Cflags: -I${includedir}
