DESCRIPTION="Terminal emulator library"
HOMEPAGE="http://${PN}.sourceforge.net/"
SRC_URI="mirror://sourceforge/${PN}/${P}.tar.gz"
SRC_DIR="${PN}"

PKG_NAMES="${PN} ${PN}0 ${PN}-devel"
libvterm_CONTENTS="usr/share"
libvterm0_CONTENTS="usr/bin"
libvterm_devel_CONTENTS="usr/include usr/lib"

DIFF_EXCLUDES="Makefile"
