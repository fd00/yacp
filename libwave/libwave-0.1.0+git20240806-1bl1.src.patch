--- origsrc/libwave/CMakeLists.txt	2025-11-23 18:14:19.000000000 +0900
+++ src/libwave/CMakeLists.txt	2025-11-23 18:21:55.612326200 +0900
@@ -46,6 +46,10 @@ target_compile_options(${PROJECT_NAME} P
     $<$<CONFIG:RELWITHDEBINFO>:${wave_compile_options_release}>
     )
 
+if (DEFINED SOVERSION)
+    set_target_properties(${PROJECT_NAME} PROPERTIES SOVERSION ${SOVERSION})
+endif()
+
 if(BUILD_TESTING AND "${CMAKE_SOURCE_DIR}" STREQUAL "${CMAKE_CURRENT_SOURCE_DIR}")
     add_subdirectory(tests/write_f32)
 endif()
@@ -115,3 +119,10 @@ if("${CMAKE_SOURCE_DIR}" STREQUAL "${CMA
     set(CPACK_SOURCE_GENERATOR "TXZ")
     include(CPack)
 endif()
+
+set(prefix ${CMAKE_INSTALL_PREFIX})
+set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+configure_file(${CMAKE_SOURCE_DIR}/libwave.pc.in ${CMAKE_BINARY_DIR}/libwave.pc @ONLY)
+install(FILES ${CMAKE_BINARY_DIR}/libwave.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/libwave/libwave.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/libwave/libwave.pc.in	2025-11-23 18:22:09.740993200 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: libwave
+Description: Simple C library for reading/writing PCM wave (.wav) files
+Version: @PROJECT_VERSION@
+Libs: -L${libdir} -lwave
+Cflags: -I${includedir}
