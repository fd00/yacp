DESCRIPTION="Simple, portable, lightweight library of audio feature extraction functions"
HOMEPAGE="http://${PN}.sf.net/"
SRC_URI="mirror://sourceforge/${PN}/${P}.tar.gz"

CYGCONF_ARGS="
	--disable-static
	--enable-fft
"

PKG_NAMES="${PN} ${PN}0 ${PN}-devel"
libxtract_CONTENTS="usr/share"
libxtract0_CONTENTS="usr/bin"
libxtract_devel_CONTENTS="usr/include usr/lib"
