--- origsrc/libzstd-seek-1.3.0/CMakeLists.txt	2026-03-08 22:31:03.000000000 +0900
+++ src/libzstd-seek-1.3.0/CMakeLists.txt	2026-07-27 09:45:48.381380100 +0900
@@ -72,11 +72,18 @@ add_library(zstd-seek zstd-seek.c zstd-s
 target_include_directories(zstd-seek PUBLIC ${CMAKE_CURRENT_SOURCE_DIR} ${ZSTD_INC})
 target_link_libraries(zstd-seek ${ZSTD_LIB})
 
+if (DEFINED SOVERSION)
+    set_target_properties(zstd-seek PROPERTIES SOVERSION ${SOVERSION})
+endif()
+
 if(DEBUG_LOG)
     target_compile_definitions(zstd-seek PRIVATE ZSTD_SEEK_DEBUG=1)
     message(STATUS "libzstd-seek: debug logging enabled (ZSTD_SEEK_DEBUG=1)")
 endif()
 
+install(TARGETS zstd-seek RUNTIME DESTINATION bin ARCHIVE DESTINATION lib)
+install(FILES zstd-seek.h DESTINATION include)
+
 option(BUILD_EXAMPLES "Build example programs" OFF)
 if(BUILD_EXAMPLES AND NOT FUZZ)
     add_subdirectory(examples)
@@ -97,3 +104,9 @@ if(FUZZ)
     message(STATUS "libzstd-seek: building fuzz harnesses with libFuzzer + ASAN + UBSAN")
     add_subdirectory(fuzz)
 endif()
+set(prefix ${CMAKE_INSTALL_PREFIX})
+set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+configure_file(${CMAKE_SOURCE_DIR}/libzstd-seek.pc.in ${CMAKE_BINARY_DIR}/libzstd-seek.pc @ONLY)
+install(FILES ${CMAKE_BINARY_DIR}/libzstd-seek.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/libzstd-seek-1.3.0/libzstd-seek.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/libzstd-seek-1.3.0/libzstd-seek.pc.in	2026-07-27 13:01:50.496868000 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: libzstd-seek
+Description: Library to read zstd files at arbitraty positions
+Version: @PROJECT_VERSION@
+Libs: -L${libdir} -lzstd-seek
+Cflags: -I${includedir}
