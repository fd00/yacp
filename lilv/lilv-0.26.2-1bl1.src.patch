--- origsrc/lilv-0.26.2/tools/lv2bench.c	2025-11-26 05:16:43.000000000 +0900
+++ src/lilv-0.26.2/tools/lv2bench.c	2026-01-23 12:11:04.947996900 +0900
@@ -228,13 +228,14 @@ bench(const LilvPlugin* const p, const O
   }
 
   if (!skip_plugin) {
+#if defined(MCL_CURRENT)
     if (options.lock_memory) {
       if (mlockall(MCL_CURRENT) != 0) {
         fprintf(stderr, "Failed to lock memory: %s\n", strerror(errno));
         return 1;
       }
     }
-
+#endif
     lilv_instance_activate(instance);
 
     if (options.realtime_priority != -1) {
