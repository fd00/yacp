--- origsrc/lilv-0.28.0/tools/lv2bench.c	2026-06-09 07:26:12.000000000 +0900
+++ src/lilv-0.28.0/tools/lv2bench.c	2026-07-27 20:17:11.391479500 +0900
@@ -228,11 +228,13 @@ bench(const LilvPlugin* const p, const O
   }
 
   if (!skip_plugin) {
+#if defined(MCL_CURRENT)
     if (options.lock_memory) {
       if (mlockall(MCL_CURRENT) != 0) {
         fprintf(stderr, "Failed to lock memory: %s\n", strerror(errno));
       }
     }
+#endif
 
     lilv_instance_activate(instance);
 
