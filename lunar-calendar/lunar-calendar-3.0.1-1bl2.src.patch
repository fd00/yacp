--- origsrc/lunar-calendar-3.0.1/lunar-calendar/meson.build	2022-01-15 22:49:13.000000000 +0900
+++ src/lunar-calendar-3.0.1/lunar-calendar/meson.build	2025-12-11 16:26:16.300089000 +0900
@@ -19,14 +19,20 @@ source_c = [
 
 install_headers(source_h, subdir : join_paths('lunar-calendar-3.0', 'lunar-calendar'))
 
+liblunar_calendar_kwargs = {
+  'soversion': soversion,
+  'version': libversion,
+  'dependencies': [gtk_dep, lunar_date_dep],
+  'include_directories': top_srcdir,
+  'install': true,
+}
+if host_machine.system() != 'cygwin'
+  liblunar_calendar_kwargs += {'install_dir': get_option('libdir')}
+endif
+
 liblunar_calendar = shared_library('lunar-calendar-3.0',
   source_c,
-  soversion: soversion,
-  version: libversion,
-  dependencies: [gtk_dep, lunar_date_dep],
-  include_directories: top_srcdir,
-  install : true,
-  install_dir : get_option('libdir')
+  kwargs: liblunar_calendar_kwargs
   )
 
 liblunar_calendar_deps = declare_dependency(sources: source_h,
