--- origsrc/lunar-date-3.2.0/lunar-date/meson.build	2025-12-02 11:28:32.000000000 +0900
+++ src/lunar-date-3.2.0/lunar-date/meson.build	2025-12-11 15:58:09.795392400 +0900
@@ -33,14 +33,21 @@ source_resources = gnome.compile_resourc
 
 install_headers(source_h, subdir : join_paths('lunar-date-3.0', 'lunar-date'))
 
+liblunar_date_kwargs = {
+  'dependencies': [gio_dep],
+  'soversion': soversion,
+  'version': libversion,
+  'include_directories': top_srcdir,
+  'install': true,
+}
+
+if host_machine.system() != 'cygwin'
+  liblunar_date_kwargs += {'install_dir': get_option('libdir')}
+endif
+
 liblunar_date = shared_library('lunar-date-3.0',
   source_c, source_enum, source_resources,
-  dependencies: [gio_dep],
-  soversion: soversion,
-  version: libversion,
-  include_directories: top_srcdir,
-  install : true,
-  install_dir : get_option('libdir')
+  kwargs: liblunar_date_kwargs
   )
 
 liblunar_date_deps = declare_dependency(sources: source_h,
