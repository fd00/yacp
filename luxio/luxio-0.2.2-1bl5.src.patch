--- origsrc/luxio-0.2.2/Makefile.am	2010-02-10 10:44:59.000000000 +0900
+++ src/luxio-0.2.2/Makefile.am	2025-12-12 14:26:26.104489500 +0900
@@ -1,6 +1,9 @@
+SUBDIRS = . test
+
 lib_LTLIBRARIES = libluxio.la
 
 libluxio_la_SOURCES = btree.cpp array.cpp data.h util.h
+libluxio_la_LDFLAGS = -no-undefined
 
 libluxio_includedir = $(includedir)/luxio
 libluxio_include_HEADERS = btree.h array.h dbm.h types.h exception.h luxio-config.h
@@ -29,3 +32,6 @@ dist-hook:
 		rm -rf $(distdir)/$$subdir/*/.svn; \
 	done
 
+
+pkgconfigdir = $(libdir)/pkgconfig
+pkgconfig_DATA = $(PACKAGE).pc
--- origsrc/luxio-0.2.2/array.cpp	2010-02-10 10:44:59.000000000 +0900
+++ src/luxio-0.2.2/array.cpp	2025-12-12 14:43:52.921225200 +0900
@@ -178,8 +178,10 @@ namespace IO {
     if (!wlock_db()) { return false; }
     off_t off = index * dh_->data_size + dh_->page_size;
     if (off + dh_->data_size > allocated_size_) {
-      div_t d = div(off + dh_->data_size, dh_->page_size);
-      uint32_t page_num = d.rem > 0 ? d.quot + 1 : d.quot;
+      off_t total_size = off + dh_->data_size;
+      uint32_t quot = total_size / dh_->page_size;
+      uint32_t rem = total_size % dh_->page_size;
+      uint32_t page_num = rem > 0 ? quot + 1 : quot;
       if (!realloc_pages(page_num, dh_->page_size)) {
         unlock_db();
         return false;
--- origsrc/luxio-0.2.2/configure.ac	2010-02-10 10:44:59.000000000 +0900
+++ src/luxio-0.2.2/configure.ac	2025-12-12 14:26:26.104489500 +0900
@@ -14,7 +14,7 @@ AC_PROG_CPP
 AC_PROG_INSTALL
 AC_PROG_LN_S
 AC_PROG_MAKE_SET
-AC_PROG_RANLIB
+LT_INIT
 
 # Checks for libraries.
 AC_PROG_LIBTOOL
@@ -40,5 +40,5 @@ AC_SYS_LARGEFILE
 AC_FUNC_MMAP
 AC_CHECK_FUNCS([ftruncate getpagesize gettimeofday memmove memset mkdir munmap strchr pread pwrite])
 
-AC_CONFIG_FILES([Makefile])
+AC_CONFIG_FILES([luxio.pc Makefile test/Makefile])
 AC_OUTPUT
--- origsrc/luxio-0.2.2/data.h	2010-02-10 10:44:59.000000000 +0900
+++ src/luxio-0.2.2/data.h	2025-12-12 14:41:44.010739500 +0900
@@ -308,8 +308,7 @@ namespace IO {
           break;
         case BLOCKALIGNED:
           {
-            div_t d = div(size, dh_->block_size);
-            uint32_t num_blocks = d.rem > 0 ? d.quot + 1 : d.quot;
+            uint32_t num_blocks = (size + dh_->block_size - 1) / dh_->block_size;
             padded_size = dh_->block_size * num_blocks;
           }
           break;
@@ -476,9 +475,8 @@ namespace IO {
 
         // calculate id and offset for the unused free space
         // and put it back to the free pools
-        div_t d = div(size, dh_->block_size);
-        uint32_t num_blocks = d.quot;
-        uint32_t off = d.rem + pool.off;
+        uint32_t num_blocks = size / dh_->block_size;
+        uint32_t off = (size % dh_->block_size) + pool.off;
         if (off >= dh_->block_size) { 
           ++num_blocks;
           off -= dh_->block_size; // must be held by uint16_t
@@ -488,8 +486,9 @@ namespace IO {
           return NULL;
         }
       } else {
-        div_t d = div(size, dh_->block_size);
-        uint32_t num_blocks = d.rem > 0 ? d.quot + 1 : d.quot;
+        uint32_t quot = size / dh_->block_size;
+        uint32_t rem = size % dh_->block_size;
+        uint32_t num_blocks = rem > 0 ? quot + 1 : quot;
         if (!extend_blocks(num_blocks)) { return NULL; }
 
         //write a record into the head of the block
@@ -501,9 +500,9 @@ namespace IO {
             return NULL;
           }
         } else {
-          if (d.rem > 0) {
-            if (!add_free_pool(dh_->cur_block_id + d.quot,
-                               d.rem, dh_->block_size - d.rem)) {
+          if (rem > 0) {
+            if (!add_free_pool(dh_->cur_block_id + quot,
+                               rem, dh_->block_size - rem)) {
               return NULL;
             }
           }
@@ -670,7 +669,7 @@ namespace IO {
       record_header_t h;
       off_t off = calc_off(data_ptr->id, data_ptr->off);
       if (!_pread(fd_, &h, sizeof(record_header_t), off)) {
-        return false;
+        return NULL;
       }
       if (h.type == AREA_FREE) { return NULL; }
 
@@ -1177,7 +1176,7 @@ namespace IO {
     data_ptr_t *put_unit(unit_t *u)
     {
       data_ptr_t *data_ptr = alloc_space(u->h->padded_size);
-      if (data_ptr == NULL) { return false; }
+      if (data_ptr == NULL) { return NULL; }
       if (!write_unit(u, data_ptr)) {
         return NULL;
       }
--- origsrc/luxio-0.2.2/luxio.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/luxio-0.2.2/luxio.pc.in	2025-12-12 14:57:00.457338000 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: luxio
+Description: Yet another fast database manager
+Version: @VERSION@
+Libs: -L${libdir} -lluxio
+Cflags: -I${includedir}
--- origsrc/luxio-0.2.2/test/Makefile.am	1970-01-01 09:00:00.000000000 +0900
+++ src/luxio-0.2.2/test/Makefile.am	2025-12-12 14:26:26.117029900 +0900
@@ -0,0 +1,34 @@
+
+check_PROGRAMS = btree_cluster_test \
+          btree_noncluster_padded_test \
+          btree_noncluster_linked_test \
+          data_test \
+          array_test \
+          btree_thread_test
+
+AM_CPPFLAGS = -D_LARGEFILE_SOURCE -D_FILE_OFFSET_BITS=64
+AM_LDFLAGS = -fprofile-arcs -ftest-coverage
+
+btree_cluster_test_SOURCES = btree_cluster_test.cpp
+btree_cluster_test_CXXFLAGS = -fprofile-arcs -ftest-coverage
+btree_cluster_test_LDADD = -lgtest $(top_builddir)/libluxio.la
+
+btree_noncluster_padded_test_SOURCES = btree_noncluster_padded_test.cpp
+btree_noncluster_padded_test_CXXFLAGS = -fprofile-arcs -ftest-coverage
+btree_noncluster_padded_test_LDADD = -lgtest $(top_builddir)/libluxio.la
+
+btree_noncluster_linked_test_SOURCES = btree_noncluster_linked_test.cpp
+btree_noncluster_linked_test_CXXFLAGS = -fprofile-arcs -ftest-coverage
+btree_noncluster_linked_test_LDADD = -lgtest $(top_builddir)/libluxio.la
+
+data_test_SOURCES = data_test.cpp
+data_test_CXXFLAGS = -fprofile-arcs -ftest-coverage
+data_test_LDADD = -lgtest $(top_builddir)/libluxio.la
+
+array_test_SOURCES = array_test.cpp
+array_test_CXXFLAGS = -fprofile-arcs -ftest-coverage
+array_test_LDADD = -lgtest $(top_builddir)/libluxio.la
+
+btree_thread_test_SOURCES = btree_thread_test.cpp
+btree_thread_test_CXXFLAGS = -fprofile-arcs -ftest-coverage
+btree_thread_test_LDADD = -lgtest $(top_builddir)/libluxio.la
--- origsrc/luxio-0.2.2/test/btree_cluster_test.cpp	2010-02-10 10:44:59.000000000 +0900
+++ src/luxio-0.2.2/test/btree_cluster_test.cpp	2025-12-12 14:26:26.119044000 +0900
@@ -532,6 +532,7 @@ namespace {
     delete bt;
   }
 
+#if !defined(__CYGWIN__) // hang up
   /* 
    * sequence: ordered
    * key: string
@@ -564,6 +565,7 @@ namespace {
     ASSERT_EQ(true, bt->close());
     delete bt;
   }
+#endif
 
   /* 
    * sequence: ordered
--- origsrc/luxio-0.2.2/util.h	2010-02-10 10:44:59.000000000 +0900
+++ src/luxio-0.2.2/util.h	2025-12-12 14:26:26.120562600 +0900
@@ -26,6 +26,7 @@
 #include <string>
 #include <iostream>
 #include <unistd.h>
+#include <cstdio>
 
 #ifdef DEBUG
 #define error_log(msg) \
