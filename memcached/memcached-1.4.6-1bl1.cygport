DESCRIPTION="Distributed memory object caching system"
HOMEPAGE="http://${PN}.org/"
SRC_URI="http://${PN}.googlecode.com/files/${P}.tar.gz"

CYGCONF_ARGS="
	--enable-sasl
	--enable-sasl-pwdb
"

src_test()
{
	cd ${B}
	./testapp.exe
}

DIFF_EXCLUDES="Makefile"
DOCS="doc/CONTRIBUTORS doc/protocol.txt"
