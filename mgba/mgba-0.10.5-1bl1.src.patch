--- origsrc/mgba-0.10.5/CMakeLists.txt	2026-01-24 20:38:08.394466400 +0900
+++ src/mgba-0.10.5/CMakeLists.txt	2026-01-24 22:19:57.064612600 +0900
@@ -580,7 +580,11 @@ if(USE_FFMPEG)
 	endif()
 endif()
 
-list(APPEND THIRD_PARTY_SRC "${CMAKE_CURRENT_SOURCE_DIR}/src/third-party/blip_buf/blip_buf.c")
+find_package(pkgconfig)
+pkg_check_modules(BLIP_BUF libblip_buf)
+if(NOT BLIP_BUF_FOUND)
+	list(APPEND THIRD_PARTY_SRC "${CMAKE_CURRENT_SOURCE_DIR}/src/third-party/blip_buf/blip_buf.c")
+endif()
 
 if(WANT_ZLIB AND NOT USE_ZLIB)
 	set(SKIP_INSTALL_ALL ON)
@@ -765,10 +769,13 @@ if(USE_ELF)
 endif()
 
 if (USE_DISCORD_RPC)
-	set(CMAKE_OSX_DEPLOYMENT_TARGET "10.7")
-	add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/src/third-party/discord-rpc discord-rpc EXCLUDE_FROM_ALL)
+	pkg_check_modules(DISCORD_RPC DiscordRPC)
+	if(NOT DISCORD_RPC_FOUND)
+		set(CMAKE_OSX_DEPLOYMENT_TARGET "10.7")
+		add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/src/third-party/discord-rpc discord-rpc EXCLUDE_FROM_ALL)
+		include_directories(AFTER ${CMAKE_CURRENT_SOURCE_DIR}/src/third-party/discord-rpc/include)
+	endif()
 	list(APPEND FEATURES DISCORD_RPC)
-	include_directories(AFTER ${CMAKE_CURRENT_SOURCE_DIR}/src/third-party/discord-rpc/include)
 endif()
 
 if(ENABLE_SCRIPTING)
@@ -922,11 +929,8 @@ if(NOT SKIP_LIBRARY)
 
 	file(MAKE_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/include/mgba-util)
 
-	target_link_libraries(${BINARY_NAME} ${DEBUGGER_LIB} ${DEPENDENCY_LIB} ${OS_LIB})
-	install(TARGETS ${BINARY_NAME} LIBRARY DESTINATION ${LIBDIR} COMPONENT lib${BINARY_NAME} NAMELINK_SKIP ARCHIVE DESTINATION ${LIBDIR} RUNTIME DESTINATION ${LIBDIR} COMPONENT lib${BINARY_NAME})
-	if(BUILD_SHARED)
-		install(TARGETS ${BINARY_NAME} LIBRARY DESTINATION ${LIBDIR} COMPONENT ${BINARY_NAME}-dev NAMELINK_ONLY)
-	endif()
+	target_link_libraries(${BINARY_NAME} ${DEBUGGER_LIB} ${DEPENDENCY_LIB} ${BLIP_BUF_LIBRARIES} ${DISCORD_RPC_LIBRARIES} ${OS_LIB})
+	install(TARGETS ${BINARY_NAME} LIBRARY DESTINATION ${LIBDIR} COMPONENT lib${BINARY_NAME} NAMELINK_SKIP ARCHIVE DESTINATION ${LIBDIR} RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR} COMPONENT lib${BINARY_NAME})
 	if(UNIX AND NOT APPLE AND NOT HAIKU)
 		install(FILES ${CMAKE_CURRENT_SOURCE_DIR}/res/mgba-16.png DESTINATION ${CMAKE_INSTALL_DATADIR}/icons/hicolor/16x16/apps RENAME io.mgba.${PROJECT_NAME}.png COMPONENT ${BINARY_NAME})
 		install(FILES ${CMAKE_CURRENT_SOURCE_DIR}/res/mgba-24.png DESTINATION ${CMAKE_INSTALL_DATADIR}/icons/hicolor/24x24/apps RENAME io.mgba.${PROJECT_NAME}.png COMPONENT ${BINARY_NAME})
@@ -1316,3 +1320,10 @@ if(NOT QUIET AND NOT LIBMGBA_ONLY)
 	message(STATUS "	Static: ${BUILD_STATIC}")
 	message(STATUS "	Shared: ${BUILD_SHARED}")
 endif()
+
+set(prefix ${CMAKE_INSTALL_PREFIX})
+set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+configure_file(${CMAKE_SOURCE_DIR}/mgba.pc.in ${CMAKE_BINARY_DIR}/mgba.pc @ONLY)
+install(FILES ${CMAKE_BINARY_DIR}/mgba.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/mgba-0.10.5/mgba.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/mgba-0.10.5/mgba.pc.in	2026-01-24 22:20:15.637675200 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: mgba
+Description: Game Boy Advance Emulator
+Version: @LIB_VERSION_STRING@
+Libs: -L${libdir} -lmgba
+Cflags: -I${includedir}
