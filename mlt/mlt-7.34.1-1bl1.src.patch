--- origsrc/mlt-7.34.1/src/modules/gdk/CMakeLists.txt	2025-11-06 00:41:13.000000000 +0900
+++ src/mlt-7.34.1/src/modules/gdk/CMakeLists.txt	2025-12-13 09:22:39.852371500 +0900
@@ -30,7 +30,7 @@ endif()
 if(TARGET PkgConfig::pango AND TARGET PkgConfig::fontconfig)
   target_sources(mltgdk PRIVATE producer_pango.c)
   target_link_libraries(mltgdk PRIVATE PkgConfig::pango PkgConfig::fontconfig PkgConfig::pangoft2)
-  if(APPLE OR MINGW)
+  if(APPLE OR MINGW OR CYGWIN)
     target_link_libraries(mltgdk PRIVATE iconv)
   endif()
   target_compile_definitions(mltgdk PRIVATE USE_PANGO)
--- origsrc/mlt-7.34.1/src/modules/opencv/filter_opencv_tracker.cpp	2025-11-06 00:41:13.000000000 +0900
+++ src/mlt-7.34.1/src/modules/opencv/filter_opencv_tracker.cpp	2025-12-13 09:23:37.652338600 +0900
@@ -145,7 +145,11 @@ static void analyze(mlt_filter filter,
     // Create tracker and initialize it
     if (!data->initialized) {
         // Build tracker
+#if CV_VERSION_MAJOR > 3
         data->tracker.reset();
+#else
+        data->tracker = cv::Ptr<cv::Tracker>();
+#endif
 #if CV_VERSION_INT > 0x040502
         data->legacyTracker.reset();
 #endif
--- origsrc/mlt-7.34.1/src/modules/plusgpl/CMakeLists.txt	2025-11-06 00:41:13.000000000 +0900
+++ src/mlt-7.34.1/src/modules/plusgpl/CMakeLists.txt	2025-12-13 09:24:08.471389800 +0900
@@ -1,5 +1,4 @@
 add_library(mltplusgpl MODULE
-  cJSON.c cJSON.h
   consumer_cbrts.c
   factory.c
   filter_burn.c
@@ -19,7 +18,7 @@ include(GenerateExportHeader)
 generate_export_header(mltplusgpl)
 target_compile_options(mltplusgpl PRIVATE ${MLT_COMPILE_OPTIONS})
 target_include_directories(mltplusgpl PRIVATE ${CMAKE_CURRENT_BINARY_DIR})
-target_link_libraries(mltplusgpl PRIVATE mlt mlt++ Threads::Threads)
+target_link_libraries(mltplusgpl PRIVATE mlt mlt++ Threads::Threads cjson)
 if(NOT MSVC)
   target_link_libraries(mltplusgpl PRIVATE m)
 endif()
--- origsrc/mlt-7.34.1/src/modules/plusgpl/consumer_cbrts.c	2025-11-06 00:41:13.000000000 +0900
+++ src/mlt-7.34.1/src/modules/plusgpl/consumer_cbrts.c	2025-12-13 09:24:33.223314100 +0900
@@ -38,6 +38,7 @@
 
 #include <strings.h>
 // includes for socket IO
+#if !defined(__CYGWIN__)
 #if (_POSIX_C_SOURCE >= 1 || _XOPEN_SOURCE || _POSIX_SOURCE) && (_POSIX_TIMERS > 0)
 #if !(defined(__FreeBSD_kernel__) && defined(__GLIBC__))
 #define CBRTS_BSD_SOCKETS 1
@@ -48,6 +49,7 @@
 #include <sys/types.h>
 #endif
 #endif
+#endif
 #include <sys/time.h>
 #include <time.h>
 
