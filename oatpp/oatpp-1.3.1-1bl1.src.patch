--- origsrc/oatpp-1.3.1/cmake/compiler-flags.cmake	2024-02-05 12:51:38.000000000 +0900
+++ src/oatpp-1.3.1/cmake/compiler-flags.cmake	2026-01-29 13:51:33.597683100 +0900
@@ -262,9 +262,11 @@ if (CMAKE_CXX_COMPILER_ID MATCHES GNU)
   add_compiler_flags(4.6 "-Werror=format-security")
   add_compiler_flags(4.6 "-Wno-format-nonliteral")
   add_compiler_flags(4.6 "-fPIE")
-  add_compiler_flags(4.6 "-Wl,-z,defs")
-  add_compiler_flags(4.6 "-Wl,-z,now")
-  add_compiler_flags(4.6 "-Wl,-z,relro")
+  if (NOT CYGWIN)
+    add_compiler_flags(4.6 "-Wl,-z,defs")
+    add_compiler_flags(4.6 "-Wl,-z,now")
+    add_compiler_flags(4.6 "-Wl,-z,relro")
+  endif ()
 endif (CMAKE_CXX_COMPILER_ID MATCHES GNU)
 
 
--- origsrc/oatpp-1.3.1/cmake/module-config.cmake.in	2024-02-05 12:51:38.000000000 +0900
+++ src/oatpp-1.3.1/cmake/module-config.cmake.in	2026-01-29 14:07:39.108324700 +0900
@@ -4,8 +4,8 @@ if(NOT TARGET oatpp::@OATPP_MODULE_NAME@
     include("${CMAKE_CURRENT_LIST_DIR}/@OATPP_MODULE_NAME@Targets.cmake")
 endif()
 
-set_and_check(@OATPP_MODULE_NAME@_INCLUDE_DIRS "${PACKAGE_PREFIX_DIR}/include/oatpp-@OATPP_MODULE_VERSION@/@OATPP_MODULE_NAME@/")
-set_and_check(@OATPP_MODULE_NAME@_LIBRARIES_DIRS "${PACKAGE_PREFIX_DIR}/@OATPP_MODULE_LIBDIR@/oatpp-@OATPP_MODULE_VERSION@/")
+set_and_check(@OATPP_MODULE_NAME@_INCLUDE_DIRS "${PACKAGE_PREFIX_DIR}/include/@OATPP_MODULE_NAME@/")
+set_and_check(@OATPP_MODULE_NAME@_LIBRARIES_DIRS "${PACKAGE_PREFIX_DIR}/@OATPP_MODULE_LIBDIR@/")
 
 set(@OATPP_MODULE_NAME@_LIBRARIES @OATPP_MODULE_LIBRARIES@)
-set(OATPP_BASE_DIR "${PACKAGE_PREFIX_DIR}/include/oatpp-@OATPP_MODULE_VERSION@/")
+set(OATPP_BASE_DIR "${PACKAGE_PREFIX_DIR}/include/oatpp/")
--- origsrc/oatpp-1.3.1/cmake/module-install.cmake	2024-02-05 12:51:38.000000000 +0900
+++ src/oatpp-1.3.1/cmake/module-install.cmake	2026-01-29 14:08:48.776152000 +0900
@@ -61,20 +61,20 @@ install(
     TARGETS ${OATPP_THIS_MODULE_TARGETS}
     EXPORT "${OATPP_MODULE_NAME}Targets"
     ARCHIVE
-        DESTINATION "${CMAKE_INSTALL_LIBDIR}/oatpp-${OATPP_MODULE_VERSION}"
+        DESTINATION "${CMAKE_INSTALL_LIBDIR}"
         COMPONENT Devel
     LIBRARY
-        DESTINATION "${CMAKE_INSTALL_LIBDIR}/oatpp-${OATPP_MODULE_VERSION}"
+        DESTINATION "${CMAKE_INSTALL_LIBDIR}"
         COMPONENT Library
     RUNTIME
-        DESTINATION "${CMAKE_INSTALL_BINDIR}/oatpp-${OATPP_MODULE_VERSION}"
+        DESTINATION "${CMAKE_INSTALL_BINDIR}"
         COMPONENT Library
     INCLUDES
-        DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}/oatpp-${OATPP_MODULE_VERSION}/${OATPP_MODULE_NAME}"
+        DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}/${OATPP_MODULE_NAME}"
 )
 
 install(DIRECTORY ${OATPP_DIRS_TO_INSTALL}
-        DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}/oatpp-${OATPP_MODULE_VERSION}/${OATPP_MODULE_NAME}"
+        DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}/${OATPP_MODULE_NAME}"
         COMPONENT Devel
         FILES_MATCHING PATTERN "*.hpp"
 )
@@ -82,7 +82,7 @@ install(DIRECTORY ${OATPP_DIRS_TO_INSTAL
 install(EXPORT "${OATPP_MODULE_NAME}Targets"
         FILE "${OATPP_MODULE_NAME}Targets.cmake"
         NAMESPACE oatpp::
-        DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/${OATPP_MODULE_NAME}-${OATPP_MODULE_VERSION}"
+        DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/${OATPP_MODULE_NAME}"
         COMPONENT Devel
 )
 
@@ -99,7 +99,7 @@ configure_package_config_file(
             "${CMAKE_CURRENT_LIST_DIR}/module-config.cmake.in"
             "${OATPP_MODULE_NAME}Config.cmake"
         INSTALL_DESTINATION
-            "${CMAKE_INSTALL_LIBDIR}/cmake/${OATPP_MODULE_NAME}-${OATPP_MODULE_VERSION}"
+            "${CMAKE_INSTALL_LIBDIR}/cmake/${OATPP_MODULE_NAME}"
         PATH_VARS
             OATPP_MODULE_NAME
             OATPP_MODULE_VERSION
@@ -113,6 +113,6 @@ install(
             "${CMAKE_CURRENT_BINARY_DIR}/${OATPP_MODULE_NAME}Config.cmake"
             "${CMAKE_CURRENT_BINARY_DIR}/${OATPP_MODULE_NAME}ConfigVersion.cmake"
         DESTINATION
-            "${CMAKE_INSTALL_LIBDIR}/cmake/${OATPP_MODULE_NAME}-${OATPP_MODULE_VERSION}"
+            "${CMAKE_INSTALL_LIBDIR}/cmake/${OATPP_MODULE_NAME}"
         COMPONENT Devel
 )
--- origsrc/oatpp-1.3.1/src/CMakeLists.txt	2024-02-05 12:51:38.000000000 +0900
+++ src/oatpp-1.3.1/src/CMakeLists.txt	2026-01-29 14:04:25.092212700 +0900
@@ -302,13 +302,16 @@ set_target_properties(oatpp PROPERTIES
         CXX_EXTENSIONS OFF
         CXX_STANDARD_REQUIRED ON
 )
+if (DEFINED SOVERSION)
+        set_target_properties(oatpp PROPERTIES SOVERSION ${SOVERSION})
+endif()
 if (MSVC)
     target_compile_options(oatpp PRIVATE /permissive-)
 endif()
 
 set(CMAKE_THREAD_PREFER_PTHREAD ON)
 
-find_package(Threads REQUIRED)
+find_package(Threads)
 
 #######################################################################################################
 ## Link external libraries
@@ -352,6 +355,9 @@ if(OATPP_LINK_TEST_LIBRARY)
 			CXX_EXTENSIONS OFF
 			CXX_STANDARD_REQUIRED ON
 	)
+        if (DEFINED SOVERSION)
+                set_target_properties(oatpp-test PROPERTIES SOVERSION ${SOVERSION})
+        endif()
 	if (MSVC)
 		target_compile_options(oatpp-test PRIVATE /permissive-)
 	endif()
