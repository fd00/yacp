--- origsrc/ompl-0.9.5-Source/src/ompl/CMakeLists.txt	2012-02-05 20:30:52.712178900 +0900
+++ src/ompl-0.9.5-Source/src/ompl/CMakeLists.txt	2012-02-05 20:32:07.796444700 +0900
@@ -70,7 +70,7 @@ else(OMPL_IN_ROS)
 endif(OMPL_IN_ROS)
 
 # install the library
-install(TARGETS ompl DESTINATION lib${LIB_SUFFIX}/)
+install(TARGETS ompl RUNTIME DESTINATION bin ARCHIVE DESTINATION lib${LIB_SUFFIX}/)
 get_target_property(OMPL_LIBRARY_LOC ompl LOCATION)
 add_custom_command(TARGET ompl POST_BUILD
     COMMAND ${CMAKE_COMMAND} -E copy "${OMPL_LIBRARY_LOC}"
--- origsrc/ompl-0.9.5-Source/src/ompl/util/Console.h	2011-10-05 09:30:36.000000000 +0900
+++ src/ompl-0.9.5-Source/src/ompl/util/Console.h	2012-02-05 20:34:37.512751400 +0900
@@ -37,6 +37,7 @@
 #ifndef OMPL_UTIL_CONSOLE_
 #define OMPL_UTIL_CONSOLE_
 
+#include <cstdio>
 #include <string>
 #include <cstdarg>
 
--- origsrc/ompl-0.9.5-Source/tests/datastructures/pdf.cpp	2011-10-05 09:30:36.000000000 +0900
+++ src/ompl-0.9.5-Source/tests/datastructures/pdf.cpp	2012-02-05 22:05:43.109943400 +0900
@@ -164,7 +164,7 @@ TEST(PDF, Statistical)
         variance += elem.second*(elem.first-mean)*(elem.first-mean);
     }
     variance /= sumWeights;
-    const double stderr = sqrt(variance/NUM_SAMPLES);
+    const double stderr_ = sqrt(variance/NUM_SAMPLES);
 
     double sampleMean = 0.0;
     ompl::RNG rand;
@@ -172,7 +172,7 @@ TEST(PDF, Statistical)
         sampleMean += p.sample(rand.uniform01());
     sampleMean /= NUM_SAMPLES;
 
-    EXPECT_NEAR(sampleMean, mean, STDERR_WIDENING_FACTOR*stderr);
+    EXPECT_NEAR(sampleMean, mean, STDERR_WIDENING_FACTOR*stderr_);
 }
 
 int main(int argc, char** argv)
