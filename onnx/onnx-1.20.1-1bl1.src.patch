--- origsrc/onnx-1.20.1/CMakeLists.txt	2026-01-10 08:10:46.000000000 +0900
+++ src/onnx-1.20.1/CMakeLists.txt	2026-01-29 23:40:05.324582600 +0900
@@ -519,7 +519,9 @@ endif()
 
 install(TARGETS
   onnx onnx_proto
-  EXPORT ONNXTargets DESTINATION ${CMAKE_INSTALL_LIBDIR})
+  EXPORT ONNXTargets
+  RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
+  ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR})
 
 install(EXPORT ONNXTargets
   DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/ONNX"
@@ -530,5 +532,9 @@ if(ONNX_BUILD_TESTS)
   include(${ONNX_ROOT}/cmake/unittest.cmake)
 endif()
 
+# https://sources.debian.org/patches/onnx/1.20.0-1/soversion.patch/
+set_target_properties(onnx PROPERTIES VERSION 1 SOVERSION 1)
+set_target_properties(onnx_proto PROPERTIES VERSION 1 SOVERSION 1)
+
 include(cmake/summary.cmake)
 onnx_print_configuration_summary()
