--- origsrc/OpenCSD-1.8.3/CMakeLists.txt	2026-06-17 22:52:12.000000000 +0900
+++ src/OpenCSD-1.8.3/CMakeLists.txt	2026-07-02 17:18:20.354048100 +0900
@@ -114,7 +114,7 @@ elseif(CMAKE_CXX_COMPILER_ID STREQUAL "G
             ${OPENCSD_LOGICAL_OP_FLAG}
             -fexceptions
         )
-        if(APPLE)
+        if(APPLE OR CYGWIN)
             set(OPENCSD_LINK_FLAGS)
         else()
             set(OPENCSD_LINK_FLAGS
@@ -387,7 +387,7 @@ else()
     opencsd_set_output_dirs(opencsd_c_api_static "lib")
 
     if(OPENCSD_FULL_PROJECT_LAYOUT)
-        if(WIN32)
+        if(WIN32 OR CYGWIN)
             add_custom_target(stage_shared_libs
                 COMMAND ${CMAKE_COMMAND} -E make_directory "${OPENCSD_BIN_OUT}"
                 COMMAND ${CMAKE_COMMAND} -E copy_if_different
