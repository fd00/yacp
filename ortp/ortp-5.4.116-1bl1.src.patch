--- origsrc/ortp-5.4.116/CMakeLists.txt	2025-12-08 21:13:19.000000000 +0900
+++ src/ortp-5.4.116/CMakeLists.txt	2026-05-26 13:12:39.443502100 +0900
@@ -22,7 +22,7 @@
 
 cmake_minimum_required(VERSION 3.22)
 
-project(Ortp VERSION 5.4.0)
+project(Ortp VERSION 5.4.116)
 
 
 set(ORTP_MAJOR_VERSION ${PROJECT_VERSION_MAJOR})
@@ -197,7 +197,7 @@ if (ENABLE_PACKAGE_SOURCE)
 endif()
 
 include(CMakePackageConfigHelpers)
-set(CMAKE_MODULES_INSTALL_DIR "${CMAKE_INSTALL_DATADIR}/${PROJECT_NAME}/cmake")
+option(CMAKE_MODULES_INSTALL_DIR "${CMAKE_INSTALL_DATADIR}/${PROJECT_NAME}/cmake")
 configure_package_config_file("cmake/${PROJECT_NAME}Config.cmake.in" "${PROJECT_BINARY_DIR}/${PROJECT_NAME}Config.cmake"
 	INSTALL_DESTINATION "${CMAKE_MODULES_INSTALL_DIR}"
 	NO_SET_AND_CHECK_MACRO
--- origsrc/ortp-5.4.116/src/rtpsession_inet.c	2025-12-08 21:13:19.000000000 +0900
+++ src/ortp-5.4.116/src/rtpsession_inet.c	2026-05-26 14:00:39.120360700 +0900
@@ -537,9 +537,10 @@ int rtp_session_set_pktinfo(RtpSession *
 
 #ifdef IPV6_RECVPKTINFO
 		optname = IPV6_RECVPKTINFO;
-#else
+#elif defined(IPV6_RECVDSTADDR)
 		optname = IPV6_RECVDSTADDR;
 #endif
+#if defined(IPV6_RECVPKTINFO) || defined(IPV6_RECVDSTADDR)
 		retval = setsockopt(session->rtp.gs.socket, IPPROTO_IPV6, optname, optval, optlen);
 		if (retval < 0) {
 			ortp_warning("Fail to set IPv6 packet info on RTP socket: %s.", getSocketError());
@@ -548,6 +549,7 @@ int rtp_session_set_pktinfo(RtpSession *
 		if (retval < 0) {
 			ortp_warning("Fail to set IPv6 packet info on RTCP socket: %s.", getSocketError());
 		}
+#endif
 	}
 
 	return retval;
@@ -1227,7 +1229,7 @@ static int rtp_sendmsg(ortp_socket_t soc
 		}
 	}
 #endif
-#ifdef IP_PKTINFO
+#if defined(IP_PKTINFO) && !defined(__CYGWIN__)
 	if (m->recv_addr.family == AF_INET || useV4 == TRUE) { // Add IPV4 to the message control
 		struct in_pktinfo *pktinfo;
 		cmsg->cmsg_len = CMSG_LEN(sizeof(struct in_pktinfo));
