--- origsrc/packmol/CMakeLists.txt	2012-02-19 13:45:14.947707700 +0900
+++ src/packmol/CMakeLists.txt	2012-02-19 13:46:45.373779700 +0900
@@ -24,9 +24,9 @@ SET(srcparallel feasyparallel.f geasypar
 
 # Compile SMP version?
 # Find_package doesn't currently support Fortran, so we need to do things by hand.
-#find_package( OpenMP )
+find_package( OpenMP )
 
-option(USE_OPENMP "Compile OpenMP enabled version?" OFF)
+option(USE_OPENMP "Compile OpenMP enabled version?" ON)
 if(USE_OPENMP)
  # Determine necessary flags
  if(CMAKE_Fortran_COMPILER_ID STREQUAL "GNU")
