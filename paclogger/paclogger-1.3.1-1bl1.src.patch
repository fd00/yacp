--- origsrc/paclogger/CMakeLists.txt	2024-05-30 09:03:02.000000000 +0900
+++ src/paclogger/CMakeLists.txt	2026-07-06 22:04:41.028820000 +0900
@@ -27,6 +27,7 @@ target_compile_definitions(${PROJECT_NAM
                            PRIVATE PL_ENV="${PACLOGGER_UPDATES_LOG}")
 
 # MOVE EXECUTABLE TO OUTPUT FOLDER
+if (NOT CYGWIN)
 add_custom_command(
   TARGET ${PROJECT_NAME}
   POST_BUILD
@@ -40,6 +41,7 @@ add_custom_command(
   COMMAND strip -x -X -s -R .gnu.version -R .gnu.version_r.
           ${CMAKE_CURRENT_BINARY_DIR}/${PROJECT_NAME}
   COMMENT "Stripping executable")
+endif()
 
 set(BINARY_OUTPUT_DIR
     "${CMAKE_INSTALL_PREFIX}/${DESTINATION_PREFIX}/${PROJECT_NAME}")
