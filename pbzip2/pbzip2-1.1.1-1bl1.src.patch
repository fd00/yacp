--- origsrc/pbzip2-1.1.1/Makefile	2010-04-18 03:37:39.000000000 +0900
+++ src/pbzip2-1.1.1/Makefile	2010-07-25 02:23:10.840384000 +0900
@@ -35,29 +35,29 @@ LDFLAGS = -lbz2
 LDFLAGS += -lpthread
 
 # Where you want pbzip2 installed when you do 'make install'
-PREFIX = /usr
+PREFIX = $(DESTDIR)/usr
 
-all: pbzip2
+all: pbzip2.exe
 
 # Standard pbzip2 compile
-pbzip2: pbzip2.cpp BZ2StreamScanner.cpp
-	$(CC) $(CFLAGS) $^ -o pbzip2 $(LDFLAGS)
+pbzip2.exe: pbzip2.cpp BZ2StreamScanner.cpp
+	$(CC) $(CFLAGS) $^ -o $@ $(LDFLAGS)
 
 # Choose this if you want to compile in a static version of the libbz2 library
 pbzip2-static: pbzip2.cpp BZ2StreamScanner.cpp libbz2.a
 	$(CC) $(CFLAGS) $^ -o pbzip2 -I. -L. $(LDFLAGS)
 
 # Install the binary pbzip2 program and man page
-install: pbzip2
+install: pbzip2.exe
 	if ( test ! -d $(PREFIX)/bin ) ; then mkdir -p $(PREFIX)/bin ; fi
-	if ( test ! -d $(PREFIX)/man ) ; then mkdir -p $(PREFIX)/man ; fi
-	if ( test ! -d $(PREFIX)/man/man1 ) ; then mkdir -p $(PREFIX)/man/man1 ; fi
-	cp -f pbzip2 $(PREFIX)/bin/pbzip2
-	chmod a+x $(PREFIX)/bin/pbzip2
-	ln -s -f $(PREFIX)/bin/pbzip2 $(PREFIX)/bin/pbunzip2
-	ln -s -f $(PREFIX)/bin/pbzip2 $(PREFIX)/bin/pbzcat
-	cp -f pbzip2.1 $(PREFIX)/man/man1
-	chmod a+r $(PREFIX)/man/man1/pbzip2.1
+	if ( test ! -d $(PREFIX)/share/man ) ; then mkdir -p $(PREFIX)/share/man ; fi
+	if ( test ! -d $(PREFIX)/share/man/man1 ) ; then mkdir -p $(PREFIX)/share/man/man1 ; fi
+	cp -f pbzip2.exe $(PREFIX)/bin/pbzip2.exe
+	chmod a+x $(PREFIX)/bin/pbzip2.exe
+	ln -s -f pbzip2.exe $(PREFIX)/bin/pbunzip2
+	ln -s -f pbzip2.exe $(PREFIX)/bin/pbzcat
+	cp -f pbzip2.1 $(PREFIX)/share/man/man1
+	chmod a+r $(PREFIX)/share/man/man1/pbzip2.1
 
 clean:
-	rm -f *.o pbzip2
+	rm -f *.o pbzip2.exe
