category: Utils
requires: cygwin libgcc1 libpoppler7 libstdc++6
sdesc: "Tool similar to grep which searches text in PDFs"
ldesc: "Pdfgrep is a tool to search text in PDF files. It works similar to
grep. 
 * search for regular expressions.
 * support for some important grep options, including:
   * filename output.
   * page number output.
   * optional case insensitivity.
   * count occurrences.
 * and the most important feature: color output!"
