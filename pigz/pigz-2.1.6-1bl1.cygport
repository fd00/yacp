DESCRIPTION="Parallel implementation of gzip"
HOMEPAGE="http://www.zlib.net/${PN}/"
SRC_URI="http://www.zlib.net/${PN}/${P}.tar.gz"

src_compile()
{
	cd ${B}
	lndirs
	cygmake
}

src_install()
{
	cd ${B}
	dobin *.exe
	doman ${PN}.1
}

DOCS="*.pdf *.ps"
