postmark
------------------------------------------
Benchmark that's based around small file operations similar to those used on
large mail servers and news servers. Has been ported to NT so should be good
for comparing OSs.

Runtime requirements:
  cygwin-1.7.35-1

Build requirements:
(besides corresponding -devel packages)
  binutils-2.25-1
  cygport-0.18.1-1
  gcc-core-4.9.2-3

Canonical website:
  https://packages.debian.org/stable/utils/postmark

Canonical download:
  mirror://debian/pool/main/p/postmark/postmark_1.51.orig.tar.gz

-------------------------------------------

Build instructions:
  1. unpack postmark-1.51-X-src.tar.xz
  2. if you use setup to install this src package,
     it will be unpacked under /usr/src automatically
        % cd /usr/src
        % cygport ./postmark-1.51-X.cygport all

This will create:
  /usr/src/postmark-1.51-X-src.tar.xz
  /usr/src/postmark-1.51-X.tar.xz

-------------------------------------------

Files included in the binary package:

(postmark)
  /usr/bin/postmark.exe
  /usr/share/doc/Cygwin/postmark.README

------------------

Port Notes:

----- version 1.51-1bl1 -----
Initial release by fd0 <https://github.com/fd00/>

