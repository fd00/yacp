--- origsrc/quvi-0.1.4/configure.ac	2010-03-28 01:08:39.000000000 +0900
+++ src/quvi-0.1.4/configure.ac	2010-05-02 17:12:13.655625000 +0900
@@ -27,18 +27,24 @@ AC_DEFINE_UNQUOTED([CANONICAL_HOST], "$h
 
 AM_INIT_AUTOMAKE([-Wall -Werror foreign dist-bzip2])
 
+AC_LIBTOOL_WIN32_DLL
 LT_INIT()
 LT_PREREQ([2.2])
 
 # Host specific.
 NO_UNDEFINED=
 case "$host" in
-    *-*-mingw32* | *-*-cygwin*)
+    *-*-mingw32*)
         AC_DEFINE([HOST_W32], [1], [...])
         host_w32=yes
         if [ test x"$enable_shared" != "xno" ]; then
             NO_UNDEFINED="-no-undefined"
         fi
+    ;;
+    *-*-cygwin*)
+        if [ test x"$enable_shared" != "xno" ]; then
+            NO_UNDEFINED="-no-undefined"
+        fi
     ;;
 esac
 EXTRA_LIBQUVI_LDFLAGS="$NO_UNDEFINED"
