--- origsrc/rawtoaces-1.0/CMakeLists.txt	2017-10-19 11:36:47.000000000 +0900
+++ src/rawtoaces-1.0/CMakeLists.txt	2019-12-19 14:08:31.115414100 +0900
@@ -1,16 +1,18 @@
 cmake_minimum_required(VERSION 2.8)
 project( RAWTOACES )
 
-set( RAWTOACES_MAJOR_VERSION 0 )
-set( RAWTOACES_MINOR_VERSION 1 )
+set( RAWTOACES_MAJOR_VERSION 1 )
+set( RAWTOACES_MINOR_VERSION 0 )
 set( RAWTOACES_PATCH_VERSION 0 )
 set( RAWTOACES_VERSION ${RAWTOACES_MAJOR_VERSION}.${RAWTOACES_MINOR_VERSION}.${RAWTOACES_PATCH_VERSION} )
 
 set(RAWTOACESIDTLIB "rawtoaces_idt")
 set(RAWTOACESLIB "rawtoaces_util")
 
-set(RAWTOACESIDTLIB ${RAWTOACESIDTLIB}.${RAWTOACES_VERSION})
-set(RAWTOACESLIB ${RAWTOACESLIB}.${RAWTOACES_VERSION})
+if (NOT CYGWIN)
+  set(RAWTOACESIDTLIB ${RAWTOACESIDTLIB}.${RAWTOACES_VERSION})
+  set(RAWTOACESLIB ${RAWTOACESLIB}.${RAWTOACES_VERSION})
+endif()
 
 set( CMAKE_MACOSX_RPATH 1 )
 
--- origsrc/rawtoaces-1.0/lib/CMakeLists.txt	2017-10-19 11:36:47.000000000 +0900
+++ src/rawtoaces-1.0/lib/CMakeLists.txt	2019-12-19 14:09:23.145588500 +0900
@@ -24,5 +24,8 @@ install(FILES
  	DESTINATION include/rawtoaces/include
 )
 
-install( TARGETS ${RAWTOACESIDTLIB} DESTINATION lib )
+install( TARGETS ${RAWTOACESIDTLIB} RUNTIME DESTINATION bin ARCHIVE DESTINATION lib )
 
+if (CYGWIN)
+  set_target_properties(${RAWTOACESIDTLIB} PROPERTIES SOVERSION ${RAWTOACES_VERSION})
+endif()
--- origsrc/rawtoaces-1.0/src/CMakeLists.txt	2017-10-19 11:36:47.000000000 +0900
+++ src/rawtoaces-1.0/src/CMakeLists.txt	2019-12-19 14:10:20.367205800 +0900
@@ -52,4 +52,8 @@ install(FILES
  	DESTINATION include/rawtoaces/include
 )
 
-install( TARGETS ${RAWTOACESLIB} DESTINATION lib )
+install( TARGETS ${RAWTOACESLIB} RUNTIME DESTINATION bin ARCHIVE DESTINATION lib )
+
+if (CYGWIN)
+  set_target_properties(${RAWTOACESLIB} PROPERTIES SOVERSION ${RAWTOACES_VERSION})
+endif()
