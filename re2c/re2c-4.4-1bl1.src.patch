--- origsrc/re2c-4.4/CMakeLists.txt	2025-12-21 05:36:43.000000000 +0900
+++ src/re2c-4.4/CMakeLists.txt	2026-01-31 20:46:32.289233100 +0900
@@ -635,6 +635,9 @@ if (RE2C_BUILD_LIBS)
     if ((NOT DEFINED BUILD_SHARED_LIBS) OR BUILD_SHARED_LIBS)
         add_library(libre2c_shared SHARED ${libre2c_sources})
         set_target_properties(libre2c_shared PROPERTIES OUTPUT_NAME "re2c")
+        if (DEFINED SOVERSION)
+            set_target_properties(libre2c_shared PROPERTIES SOVERSION "${SOVERSION}")
+        endif()
         if (UNIX)
             install(TARGETS libre2c_shared LIBRARY DESTINATION lib)
         endif()
@@ -677,3 +680,10 @@ if(RE2C_BUILD_TESTS)
     add_custom_target(check)
     add_dependencies(check check_re2c check_libre2c)
 endif()
+
+set(prefix ${CMAKE_INSTALL_PREFIX})
+set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+configure_file(${CMAKE_SOURCE_DIR}/re2c.pc.in ${CMAKE_BINARY_DIR}/re2c.pc @ONLY)
+install(FILES ${CMAKE_BINARY_DIR}/re2c.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/re2c-4.4/re2c.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/re2c-4.4/re2c.pc.in	2026-01-31 20:46:32.289233100 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: re2c
+Description: Free and open-source lexer generator for C, C++ and Go
+Version: @PROJECT_VERSION@
+Libs: -L${libdir} -lre2c
+Cflags: -I${includedir}
