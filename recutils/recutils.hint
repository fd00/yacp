category: Text
requires: cygwin libgcrypt11 libintl8 libmdb1 libreadline7 librec0
sdesc: "Tools and libraries to access recfiles (utilities)"
ldesc: "Recutils is a set of tools and libraries to access human-editable,
text-based databases called recfiles. The data is stored as a sequence of
records, each record containing an arbitrary number of named fields."
