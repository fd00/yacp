--- origsrc/repaq-0.5.1/Makefile	2025-05-22 08:00:22.000000000 +0900
+++ src/repaq-0.5.1/Makefile	2026-07-12 18:29:24.012564300 +0900
@@ -14,8 +14,8 @@ TARGET := repaq
 
 BIN_TARGET := ${TARGET}
 
-CXX := g++
-CXXFLAGS := -std=c++11 -g -I${DIR_INC} $(foreach includedir,$(INCLUDE_DIRS),-I$(includedir))
+CXX ?= g++
+CXXFLAGS ?= -std=c++11 -g -I${DIR_INC} $(foreach includedir,$(INCLUDE_DIRS),-I$(includedir))
 LIBS := -lz -lpthread
 LD_FLAGS := $(foreach librarydir,$(LIBRARY_DIRS),-L$(librarydir)) $(LIBS)
 
@@ -24,7 +24,7 @@ ${BIN_TARGET}:${OBJ}
 	$(CXX) $(OBJ) -o $@ $(LD_FLAGS)
 
 ${DIR_OBJ}/%.o:${DIR_SRC}/%.cpp make_obj_dir
-	$(CXX) $(CXXFLAGS) -O3 -c $< -o $@
+	$(CXX) $(CXXFLAGS) -c $< -o $@
 
 .PHONY:clean
 clean:
