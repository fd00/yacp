roundup
------------------------------------------
roundup(1) is a unit testing tool for running roundup(5) test plans which are
written in any POSIX shell.  Each test in a plan is run in its own isolated
sandbox.  A test can pass, be ignored, or fail.  Failed tests output their
`set -x` trace.

Runtime requirements:
  bash-4.1.10-4

Build requirements:
(besides corresponding -devel packages)
  cygport-0.11.3-1
  make-3.82.90-1

Canonical website:
  http://bmizerany.github.com/roundup/

Canonical download:
  git://github.com/bmizerany/roundup.git

-------------------------------------------

Build instructions:
  unpack roundup-0.0.5-X-src.tar.bz2
    if you use setup to install this src package, it will be
         unpacked under /usr/src automatically
  cd /usr/src
  cygport ./roundup-0.0.5-X.cygport all

This will create:
  /usr/src/roundup-0.0.5-X-src.tar.bz2
  /usr/src/roundup-0.0.5-X.tar.bz2

-------------------------------------------

Files included in the binary package:

(roundup)
  /usr/bin/roundup
  /usr/share/doc/Cygwin/roundup.README
  /usr/share/doc/roundup/AUTHORS
  /usr/share/doc/roundup/COPYING
  /usr/share/doc/roundup/README
  /usr/share/doc/roundup/html/index.html

------------------

Port Notes:

----- version 0.0.5-1bl1 -----
Initial release for Cygwin-1.7 by fd0 <http://fd0.hatenablog.jp/>

