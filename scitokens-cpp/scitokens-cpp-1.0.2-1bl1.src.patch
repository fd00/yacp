--- origsrc/scitokens-cpp/CMakeLists.txt	2023-08-25 09:38:38.000000000 +0900
+++ src/scitokens-cpp/CMakeLists.txt	2023-08-25 09:50:21.435274800 +0900
@@ -71,7 +71,7 @@ target_link_libraries(scitokens-create S
 get_directory_property(TARGETS BUILDSYSTEM_TARGETS)
 install(
   TARGETS ${TARGETS} 
-  LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
+  ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
   RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR} )
 
 install(
@@ -97,3 +97,10 @@ endif()
 enable_testing()
 add_subdirectory(test)
 endif()
+
+set(prefix ${CMAKE_INSTALL_PREFIX})
+set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+configure_file(${CMAKE_SOURCE_DIR}/scitokens-cpp.pc.in ${CMAKE_BINARY_DIR}/scitokens-cpp.pc @ONLY)
+install(FILES ${CMAKE_BINARY_DIR}/scitokens-cpp.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/scitokens-cpp/scitokens-cpp.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/scitokens-cpp/scitokens-cpp.pc.in	2023-08-25 09:50:59.666633600 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: scitokens-cpp
+Description: C++ implementation of the SciTokens library
+Version: @PROJECT_VERSION@
+Libs: -L${libdir} -lSciTokens
+Cflags: -I${includedir}
