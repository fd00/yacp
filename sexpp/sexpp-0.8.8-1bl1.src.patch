--- origsrc/sexpp-0.8.8/CMakeLists.txt	2024-06-17 05:04:27.000000000 +0900
+++ src/sexpp-0.8.8/CMakeLists.txt	2024-09-07 19:08:01.113495000 +0900
@@ -291,12 +291,12 @@ configure_file(
     @ONLY
 )
 
-if (WIN32 AND BUILD_SHARED_LIBS)
+if ((WIN32 OR CYGWIN) AND BUILD_SHARED_LIBS)
     install(TARGETS sexpp RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}")
     install(TARGETS sexpp ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}")
-else(WIN32 AND BUILD_SHARED_LIBS)
+else((WIN32 OR CYGWIN) AND BUILD_SHARED_LIBS)
     install(TARGETS sexpp DESTINATION "${CMAKE_INSTALL_LIBDIR}")
-endif(WIN32 AND BUILD_SHARED_LIBS)
+endif((WIN32 OR CYGWIN) AND BUILD_SHARED_LIBS)
 
 install(DIRECTORY include/ DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}")
 install(FILES "${CONFIGURED_PC}" DESTINATION "${CMAKE_INSTALL_LIBDIR}/pkgconfig")
