--- origsrc/sgEvolver/configure.ac	2009-03-31 03:24:57.000000000 +0900
+++ src/sgEvolver/configure.ac	2025-08-25 01:41:24.581660900 +0900
@@ -43,14 +43,6 @@ PKG_CHECK_MODULES(DEPS, libMems-1.6 >= 1
 AC_SUBST(DEPS_CFLAGS)
 AC_SUBST(DEPS_LIBS)
 
-dnl Mac OS X won't allow static compilation...
-STATIC_FLAG="-static -Wl,--whole-archive -lpthread -Wl,--no-whole-archive"
-if ( test "x$target_vendor" = "xapple") then
-        STATIC_FLAG=""
-fi
-AC_SUBST(STATIC_FLAG)
-
-
 dnl Allow debugging compilation
 AC_ARG_ENABLE(debug,
 [  --enable-debug    Turn on debugging],
--- origsrc/sgEvolver/src/Makefile.am	2010-04-30 06:36:47.000000000 +0900
+++ src/sgEvolver/src/Makefile.am	2025-08-25 01:41:14.940918600 +0900
@@ -5,7 +5,7 @@ else
 endif
 OPTIMIZATION = -O3 -funroll-loops -fomit-frame-pointer -ftree-vectorize 
 AM_CXXFLAGS = -DCOMMAND_LINE $(OPTIMIZATION) $(D_CXXFLAGS) 
-AM_LDFLAGS = @STATIC_FLAG@ 
+AM_LDFLAGS = 
 
 bin_PROGRAMS = sgEvolver scoreAlignment2 mfaToMultiFiles maf2xmfa
 sgEvolver_SOURCES = \
