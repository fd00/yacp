--- origsrc/simdjson-2.0.3/CMakeLists.txt	2022-06-03 02:57:46.000000000 +0900
+++ src/simdjson-2.0.3/CMakeLists.txt	2022-06-14 18:05:46.497935400 +0900
@@ -280,3 +280,10 @@ if(NOT SIMDJSON_SANITIZE)
     )
   endif()
 endif()
+
+set(prefix ${CMAKE_INSTALL_PREFIX})
+set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+configure_file(${CMAKE_SOURCE_DIR}/simdjson.pc.in ${CMAKE_BINARY_DIR}/simdjson.pc @ONLY)
+install(FILES ${CMAKE_BINARY_DIR}/simdjson.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/simdjson-2.0.3/simdjson.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/simdjson-2.0.3/simdjson.pc.in	2022-06-14 18:04:44.036397800 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: simdjson
+Description: Parsing gigabytes of JSON per second
+Version: @PROJECT_VERSION@
+Libs: -L${libdir} -lsimdjson
+Cflags: -I${includedir}
