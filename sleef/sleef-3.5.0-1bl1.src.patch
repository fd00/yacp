--- origsrc/sleef-3.5.0/src/dft/CMakeLists.txt	2020-09-01 10:51:12.000000000 +0900
+++ src/sleef-3.5.0/src/dft/CMakeLists.txt	2020-09-10 21:05:51.908200100 +0900
@@ -361,5 +361,5 @@ set_target_properties(${TARGET_LIBDFT} P
 # Install
 install(TARGETS ${TARGET_LIBDFT}
         PUBLIC_HEADER DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}"
-        LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}"
+        RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}"
         ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}")
--- origsrc/sleef-3.5.0/src/libm/CMakeLists.txt	2020-09-01 10:51:12.000000000 +0900
+++ src/sleef-3.5.0/src/libm/CMakeLists.txt	2020-09-10 21:31:54.311595800 +0900
@@ -25,6 +25,8 @@ endforeach()
 if((MSVC OR MINGW AND WIN32) OR SLEEF_CLANG_ON_WINDOWS)
   string(REPLACE "/" "\\" sleef_footer_input_file "${SLEEF_ORG_FOOTER}")
   list(APPEND SLEEF_HEADER_COMMANDS COMMAND type ${sleef_footer_input_file} >> ${SLEEF_INCLUDE_HEADER})
+elseif(CYGWIN)
+  list(APPEND SLEEF_HEADER_COMMANDS COMMAND /usr/bin/cat.exe ${SLEEF_ORG_FOOTER} >> ${SLEEF_INCLUDE_HEADER})
 else()
   list(APPEND SLEEF_HEADER_COMMANDS COMMAND cat ${SLEEF_ORG_FOOTER} >> ${SLEEF_INCLUDE_HEADER})
 endif()
@@ -390,7 +392,7 @@ if(BUILD_INLINE_HEADERS)
   add_library(${TARGET_LIBINLINE} STATIC rempitab.c)
   install(TARGETS ${TARGET_LIBINLINE}
           PUBLIC_HEADER DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}"
-          LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}"
+          RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}"
           ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}")
 endif(BUILD_INLINE_HEADERS)
 
@@ -617,7 +619,7 @@ endif()
 # Install libsleef and sleef.h
 install(TARGETS ${TARGET_LIBSLEEF}
         PUBLIC_HEADER DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}"
-        LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}"
+        RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}"
         ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}")
 configure_file("sleef.pc.in" "${CMAKE_CURRENT_BINARY_DIR}/sleef.pc" @ONLY)
 install(FILES "${CMAKE_CURRENT_BINARY_DIR}/sleef.pc"
@@ -625,6 +627,6 @@ install(FILES "${CMAKE_CURRENT_BINARY_DI
 
 if(ENABLE_GNUABI)
   install(TARGETS ${TARGET_LIBSLEEFGNUABI}
-          LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}"
+          RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}"
           ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}")
 endif()
--- origsrc/sleef-3.5.0/src/quad/CMakeLists.txt	2020-09-01 10:51:12.000000000 +0900
+++ src/sleef-3.5.0/src/quad/CMakeLists.txt	2020-09-10 21:32:13.614216500 +0900
@@ -62,6 +62,8 @@ endforeach()
 if((MSVC OR MINGW AND WIN32) OR SLEEF_CLANG_ON_WINDOWS)
   string(REPLACE "/" "\\" sleef_footer_input_file "${SLEEFQUAD_ORG_FOOTER}")
   list(APPEND SLEEF_HEADER_COMMANDS COMMAND type ${sleef_footer_input_file} >> ${SLEEFQUAD_INCLUDE_HEADER})
+elseif(CYGWIN)
+  list(APPEND SLEEF_HEADER_COMMANDS COMMAND /usr/bin/cat.exe ${SLEEFQUAD_ORG_FOOTER} >> ${SLEEFQUAD_INCLUDE_HEADER})
 else()
   list(APPEND SLEEF_HEADER_COMMANDS COMMAND cat ${SLEEFQUAD_ORG_FOOTER} >> ${SLEEFQUAD_INCLUDE_HEADER})
 endif()
@@ -152,5 +154,5 @@ endif()
 # Install libsleef and sleef.h
 install(TARGETS sleefquad
         PUBLIC_HEADER DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}"
-        LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}"
+        RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}"
         ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}")
