category: Utils
requires: cygwin
sdesc: "Log a program's output to the system log"
ldesc: "The stdsyslog utility executes a program, reads everything that it
outputs to a set of file descriptors (by default the standard output and
standard error streams) and logs it to the system log."
