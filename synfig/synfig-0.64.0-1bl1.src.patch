--- origsrc/synfig-0.64.0/configure.ac	2013-09-13 07:52:34.264927400 +0900
+++ src/synfig-0.64.0/configure.ac	2013-09-13 08:23:19.203404000 +0900
@@ -160,11 +160,11 @@ AC_ARG_WITH(magickpp,[
 ])
 if test $with_magickpp = "yes" ; then {
   AC_CHECK_HEADER([Magick++.h],[
-  	MAGICKPP_LIBS="-lMagick++"
+  	MAGICKPP_LIBS=`Magick++-config --libs`
   	AC_DEFINE(HAVE_MAGICKPP,[], [ Define if Magick++ library is available ] )
   	AC_SUBST(MAGICKPP_LIBS)
   	AM_CONDITIONAL(HAVE_LIBMAGICKPP,true)
-	AC_CHECK_LIB(Magick,OptimizeImageTransparency,[
+	AC_CHECK_LIB(MagickCore,OptimizeImageTransparency,[
 		AC_DEFINE(HAVE_MAGICK_OPTIMIZE,[], [ Define if Magick++ defines OptimizeImageTransparency() ] )
 		AM_CONDITIONAL(HAVE_MAGICK_OPTIMIZE,true)
 	],[
@@ -439,8 +439,8 @@ PKG_CHECK_MODULES(LIBPANGO, pango pangoc
 	AC_MSG_ERROR([ ** You need to install pango])
 ])
 
-AX_BOOST_BASE([1.32.0])
-AX_BOOST_PROGRAM_OPTIONS
+BOOST_REQUIRE([1.32.0])
+BOOST_PROGRAM_OPTIONS
 if test "x$ax_cv_boost_program_options" = "xno" ; then
 	AC_MSG_ERROR([ ** You need to install Boost program-options library])
 fi
@@ -579,7 +579,7 @@ AC_CHECK_HEADERS(signal.h termios.h sys/
 
 
 case "$host" in
-	*mingw* | *cygwin*)
+	*mingw*)
 		AM_CONDITIONAL(WIN32_PKG, true)
 		AM_CONDITIONAL(MACOSX_PKG, false)
 	;;
@@ -761,7 +761,5 @@ Using OpenEXR's \"half\" type ------> $u
 "'$'"LIBSIGC_LIBS --------------------> '$LIBSIGC_LIBS'
 "'$'"SYNFIG_CFLAGS -------------------> '$SYNFIG_CFLAGS'
 "'$'"BOOST_CPPFLAGS ------------------> '$BOOST_CPPFLAGS'
-"'$'"BOOST_LDFLAGS -------------------> '$BOOST_LDFLAGS'
-"'$'"BOOST_PROGRAM_OPTIONS_LIB -------> '$BOOST_PROGRAM_OPTIONS_LIB'
 "'$'"LIBADD_DL -----------------------> '$LIBADD_DL'
 "
--- origsrc/synfig-0.64.0/m4/subs.m4	2013-05-10 01:32:41.000000000 +0900
+++ src/synfig-0.64.0/m4/subs.m4	2013-09-13 08:23:05.771763400 +0900
@@ -216,7 +216,7 @@ case "$host" in
 dnl    LDFLAGS="$LDFLAGS -lole32 -no-undefined -Wl,--export-all-symbols -Wl,--enable-auto-import -Wl,--subsystem=console -Wl,--enable-runtime-pseudo-reloc"
     ;;
   *cygwin*)
-    LDFLAGS="$LDFLAGS -lole32 -no-undefined -Wl,--export-all-symbols"
+    LDFLAGS="$LDFLAGS -lole32 -Wl,-no-undefined -Wl,--export-all-symbols"
 dnl    LDFLAGS="$LDFLAGS -lole32 -no-undefined -Wl,--export-all-symbols -Wl,--enable-auto-import -Wl,--subsystem=console"
     CXXFLAGS="$CXXFLAGS -I/target/include"
     CFLAGS="$CFLAGS -I/target/include"
--- origsrc/synfig-0.64.0/src/synfig/module.cpp	2013-05-10 01:32:41.000000000 +0900
+++ src/synfig-0.64.0/src/synfig/module.cpp	2013-09-13 07:52:39.983677400 +0900
@@ -127,6 +127,7 @@ synfig::Module::Register(const String &m
 
 	module=lt_dlopenext((string("lib")+module_name).c_str());
 	if(!module)module=lt_dlopenext(module_name.c_str());
+	if(!module)module=lt_dlopenext((string("cyg")+module_name).c_str());
 
 	if(!module)
 	{
--- origsrc/synfig-0.64.0/src/tool/Makefile.am	2013-05-10 01:32:41.000000000 +0900
+++ src/synfig-0.64.0/src/tool/Makefile.am	2013-09-14 05:33:46.251031300 +0900
@@ -34,8 +34,7 @@ synfig_LDADD = \
 	../synfig/libsynfig.la \
 	@SYNFIG_LIBS@ \
 	@OPENEXR_HALF_LIBS@ \
-	@BOOST_LDFLAGS@ \
-	@BOOST_PROGRAM_OPTIONS_LIB@
+	@BOOST_PROGRAM_OPTIONS_LIBS@
 
 synfig_CXXFLAGS = \
 	@SYNFIG_CFLAGS@
