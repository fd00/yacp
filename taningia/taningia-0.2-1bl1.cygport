DESCRIPTION="Generic comunication library based on XMPP"
HOMEPAGE="http://projects.comum.org/${PN}"
SRC_URI="http://bau.comum.org/lincoln/${PN}/${P}.tar.gz"

CYGCONF_ARGS="--disable-static"

PKG_NAMES="${PN} lib${PN}0 lib${PN}-devel"
taningia_CONTENTS="usr/share"
libtaningia0_CONTENTS="usr/bin"
libtaningia_devel_CONTENTS="usr/include usr/lib"
