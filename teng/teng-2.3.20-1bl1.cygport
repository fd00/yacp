HOMEPAGE="https://github.com/seznam/${PN}"
SRC_URI="https://github.com/seznam/${PN}/archive/v${PV}.tar.gz"

CATEGORY="Libs"
SUMMARY="General purpose templating system"
DESCRIPTION="${SUMMARY}"

PKG_NAMES="
	${PN}
	lib${PN}2
	lib${PN}-devel
"
teng_CONTENTS="
	usr/share
"
libteng2_CONTENTS="
	usr/bin
"
libteng_devel_CONTENTS="
	usr/include
	usr/lib
"
teng_SUMMARY="${SUMMARY} (licensing & readmes)"
libteng2_SUMMARY="${SUMMARY} (runtime)"
libteng_devel_SUMMARY="${SUMMARY} (development)"
