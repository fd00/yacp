--- origsrc/tinydtls/CMakeLists.txt	2026-01-16 13:04:11.000000000 +0900
+++ src/tinydtls/CMakeLists.txt	2026-01-16 13:13:47.552512700 +0900
@@ -41,8 +41,15 @@ endif()
 
 set(PACKAGE_NAME "tinydtls")
 set(PACKAGE_VERSION "0.8.6" )
+set(PACKAGE_URL "https://projects.eclipse.org/projects/iot.tinydtls")
 set(SOVERSION "0" )
 
+include(GNUInstallDirs)
+set(prefix ${CMAKE_INSTALL_PREFIX})
+set(exec_prefix \${prefix})
+set(includedir \${prefix}/${CMAKE_INSTALL_INCLUDEDIR})
+set(libdir \${prefix}/${CMAKE_INSTALL_LIBDIR})
+
 if(NOT ZEPHYR_BASE)
    option(DTLS_ECC "disable/enable support for TLS_ECDHE_ECDSA_WITH_AES_128_CCM_8" ON )
    option(DTLS_PSK "disable/enable support for TLS_PSK_WITH_AES_128_CCM_8" ON)
@@ -53,6 +60,7 @@ endif()
 option(WARNING_TO_ERROR "force all compiler warnings to be errors" OFF)
 
 configure_file(dtls_config.h.cmake.in dtls_config.h )
+configure_file(tinydtls.pc.in tinydtls.pc @ONLY)
 
 add_library(tinydtls)
 
@@ -100,6 +108,8 @@ else()
     install(TARGETS    tinydtls       DESTINATION ${CMAKE_INSTALL_LIBDIR} )
 endif()
 
+install(FILES ${CMAKE_CURRENT_BINARY_DIR}/tinydtls.pc DESTINATION ${CMAKE_INSTALL_LIBDIR}/pkgconfig)
+
 if(NOT (${CMAKE_VERSION} VERSION_LESS "3.18.0"))
     file(CONFIGURE OUTPUT .gitignore
         NEWLINE_STYLE UNIX
--- origsrc/tinydtls/tinydtls.pc.in	2026-01-16 13:04:11.000000000 +0900
+++ src/tinydtls/tinydtls.pc.in	2026-01-16 13:38:01.496728800 +0900
@@ -23,4 +23,4 @@ Description: A C library for Datagram Tr
 URL: @PACKAGE_URL@
 Version: @PACKAGE_VERSION@
 Libs: -L${libdir} -ltinydtls
-Cflags: -I${includedir}/tinydtls
+Cflags: -I${includedir}
