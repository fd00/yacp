DESCRIPTION="C preprocessor designed to be embeddable, quick, light and fully compliant to C99"
HOMEPAGE="http://code.google.com/p/${PN}/"
SRC_URI="http://${PN}.googlecode.com/files/${P}.tar.gz"
SRC_DIR="${PN}"

DIFF_EXCLUDES="Makefile"

PKG_NAMES="${PN} lib${PN}0 lib${PN}-devel"
ucpp_CONTENTS="usr/bin/*.exe usr/share"
libucpp0_CONTENTS="usr/bin/*.dll"
libucpp_devel_CONTENTS="usr/include usr/lib"
