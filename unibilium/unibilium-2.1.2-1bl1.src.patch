--- origsrc/unibilium-2.1.2/CMakeLists.txt	2024-09-13 18:47:46.000000000 +0900
+++ src/unibilium-2.1.2/CMakeLists.txt	2025-09-03 00:06:23.363170100 +0900
@@ -6,6 +6,10 @@ add_library(unibilium ${SRC_FILES})
 set_target_properties(unibilium PROPERTIES PUBLIC_HEADER ${PROJECT_SOURCE_DIR}/unibilium.h
   VERSION "${VERSION_MAJOR}.${VERSION_MINOR}")
 
+if (DEFINED SOVERSION)
+  set_target_properties(unibilium PROPERTIES SOVERSION ${SOVERSION})
+endif()
+
 if(NOT WIN32)
   execute_process(COMMAND sh -c "ncursesw6-config --terminfo-dirs 2>/dev/null || \
       ncurses6-config  --terminfo-dirs 2>/dev/null || \
@@ -24,5 +28,12 @@ target_compile_definitions(unibilium PUB
 include(GNUInstallDirs)
 install(TARGETS unibilium
   PUBLIC_HEADER
+  RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
   ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
   PUBLIC_HEADER DESTINATION ${CMAKE_INSTALL_INCLUDEDIR})
+
+set(INCDIR /usr/include)
+set(LIBDIR /usr/lib)
+set(VERSION ${PROJECT_VERSION})
+configure_file(${CMAKE_SOURCE_DIR}/unibilium.pc.in ${CMAKE_BINARY_DIR}/unibilium.pc @ONLY)
+install(FILES ${CMAKE_BINARY_DIR}/unibilium.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
