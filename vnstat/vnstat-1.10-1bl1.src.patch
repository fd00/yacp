--- origsrc/vnstat-1.10/Makefile	2009-08-02 04:23:43.000000000 +0900
+++ src/vnstat-1.10/Makefile	2010-07-25 18:22:46.227130300 +0900
@@ -23,10 +23,10 @@ install:
 	@echo "Installing vnStat..."
 
 # check that system is really Linux
-	@if [ `uname` != "Linux" ]; \
-	then echo "This isn't a Linux system. Maybe 'make bsdinstall' is what you need?"; \
-	false; \
-	fi
+#	@if [ `uname` != "Linux" ]; \
+#	then echo "This isn't a Linux system. Maybe 'make bsdinstall' is what you need?"; \
+#	false; \
+#	fi
 
 # check that there's something to install
 	@if [ ! -f "src/vnstat" ] || [ ! -f "src/vnstatd" ]; \
