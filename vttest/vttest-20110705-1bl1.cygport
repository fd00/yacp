DESCRIPTION="Test the compatibility of so-called 'VT100-compatible' terminals"
HOMEPAGE="http://invisible-island.net/${PN}/"
SRC_URI="ftp://invisible-island.net/${PN}/${P}.tgz"

DIFF_EXCLUDES="config.hin"
