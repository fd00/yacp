--- origsrc/yajl-fort-1.4/CMakeLists.txt	2025-10-01 02:18:46.000000000 +0900
+++ src/yajl-fort-1.4/CMakeLists.txt	2026-06-24 09:47:54.261676400 +0900
@@ -53,3 +53,10 @@ if(BUILD_HTML)
   find_package(Sphinx REQUIRED)
   add_subdirectory(doc/sphinx)
 endif()
+
+set(prefix ${CMAKE_INSTALL_PREFIX})
+set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+configure_file(${CMAKE_SOURCE_DIR}/yajl-fort.pc.in ${CMAKE_BINARY_DIR}/yajl-fort.pc @ONLY)
+install(FILES ${CMAKE_BINARY_DIR}/yajl-fort.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/yajl-fort-1.4/src/CMakeLists.txt	2025-10-01 02:18:46.000000000 +0900
+++ src/yajl-fort-1.4/src/CMakeLists.txt	2026-06-24 09:00:06.702144800 +0900
@@ -8,6 +8,10 @@ add_library(yajl_fort ${SRC})
 set_target_properties(yajl_fort PROPERTIES
     Fortran_MODULE_DIRECTORY ${LIB_MOD_DIR})
 
+if (DEFINED SOVERSION)
+    set_target_properties(yajl_fort PROPERTIES SOVERSION "${SOVERSION}")
+endif()
+
 target_include_directories(yajl_fort PUBLIC
     $<BUILD_INTERFACE:${LIB_MOD_DIR}>
     $<INSTALL_INTERFACE:lib>
@@ -19,7 +23,8 @@ target_link_libraries(yajl_fort YAJL::YA
 
 install(TARGETS yajl_fort
         EXPORT  yajl_fort
+        RUNTIME DESTINATION bin
         LIBRARY DESTINATION lib
         ARCHIVE DESTINATION lib
 )
-install(DIRECTORY ${LIB_MOD_DIR} DESTINATION lib)
+install(DIRECTORY ${LIB_MOD_DIR} DESTINATION include/yajl)
--- origsrc/yajl-fort-1.4/yajl-fort.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/yajl-fort-1.4/yajl-fort.pc.in	2026-06-24 12:34:09.425069100 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: yajl-fort
+Description: Modern Fortran interface to the YAJL library
+Version: @VERSION@
+Libs: -L${libdir} -lyajl_fort
+Cflags: -I${includedir}/yajl
