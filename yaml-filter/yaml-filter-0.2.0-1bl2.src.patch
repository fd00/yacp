--- origsrc/yaml-filter-0.2.0/CMakeLists.txt	2020-09-29 05:36:56.000000000 +0900
+++ src/yaml-filter-0.2.0/CMakeLists.txt	2026-06-25 16:57:52.137133800 +0900
@@ -11,8 +11,14 @@ include_directories(${YAML_INCLUDE_DIRS}
 
 add_library(yaml-path src/yaml-path.c)
 target_link_libraries(yaml-path ${YAML_LIBRARIES})
+if (DEFINED SOVERSION)
+	set_target_properties(yaml-path PROPERTIES SOVERSION ${SOVERSION})
+endif()
 add_coverage(yaml-path)
 
+install(TARGETS yaml-path RUNTIME DESTINATION bin ARCHIVE DESTINATION lib)
+install(FILES src/yaml-path.h DESTINATION include)
+
 add_executable(yamlp src/yamlp.c)
 target_link_libraries(yamlp yaml-path)
 add_coverage(yamlp)
@@ -39,3 +45,9 @@ add_subdirectory("tests")
 
 coverage_evaluate()
 
+set(prefix ${CMAKE_INSTALL_PREFIX})
+set(exec_prefix ${CMAKE_INSTALL_PREFIX})
+set(libdir ${CMAKE_INSTALL_PREFIX}/lib)
+set(includedir ${CMAKE_INSTALL_PREFIX}/include)
+configure_file(${CMAKE_SOURCE_DIR}/yaml-filter.pc.in ${CMAKE_BINARY_DIR}/yaml-filter.pc @ONLY)
+install(FILES ${CMAKE_BINARY_DIR}/yaml-filter.pc DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/pkgconfig)
--- origsrc/yaml-filter-0.2.0/yaml-filter.pc.in	1970-01-01 09:00:00.000000000 +0900
+++ src/yaml-filter-0.2.0/yaml-filter.pc.in	2026-06-25 18:03:44.095934500 +0900
@@ -0,0 +1,10 @@
+prefix=@prefix@
+exec_prefix=@exec_prefix@
+libdir=@libdir@
+includedir=@includedir@
+
+Name: yaml-filter
+Description: YAML document filtering for libyaml
+Version: @VERSION@
+Libs: -L${libdir} -lyaml-path
+Cflags: -I${includedir}
