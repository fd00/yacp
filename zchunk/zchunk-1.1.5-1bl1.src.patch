diff -buprN -x CYGWIN-PATCHES -x aclocal.m4~ -x aclocal.m4t -x autom4te.cache -x config.cache -x config.guess -x config.log -x config.status -x config.sub -x '*SlackBuild*' -x '*.egg-info' -x '*.dll' -x '*.exe' -x '*.o' -x '*.gch' -x '*.gem' -x .gitignore -x '*.jpg' -x '*.png' -x '*.qm' -x '*.svgz' -x '*.tif' -x '*.class' -x '*.jlc' -x '*.pyc' -x '*.pyo' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*.whl' -x '*~' -x '*.stackdump' -x x86_64-pc-cygwin -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x mate-doc-utils.make -x mate-doc-utils.m4 origsrc/zchunk-1.1.5/include/zck.h.in src/zchunk-1.1.5/include/zck.h.in
--- origsrc/zchunk-1.1.5/include/zck.h.in	2020-01-19 06:32:09.000000000 +0900
+++ src/zchunk-1.1.5/include/zck.h.in	2020-01-21 09:25:43.134343600 +0900
@@ -5,6 +5,7 @@
 
 #include <stdlib.h>
 #include <stdbool.h>
+#include <sys/types.h>
 
 typedef enum zck_hash {
     ZCK_HASH_SHA1,
diff -buprN -x CYGWIN-PATCHES -x aclocal.m4~ -x aclocal.m4t -x autom4te.cache -x config.cache -x config.guess -x config.log -x config.status -x config.sub -x '*SlackBuild*' -x '*.egg-info' -x '*.dll' -x '*.exe' -x '*.o' -x '*.gch' -x '*.gem' -x .gitignore -x '*.jpg' -x '*.png' -x '*.qm' -x '*.svgz' -x '*.tif' -x '*.class' -x '*.jlc' -x '*.pyc' -x '*.pyo' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*.whl' -x '*~' -x '*.stackdump' -x x86_64-pc-cygwin -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x mate-doc-utils.make -x mate-doc-utils.m4 origsrc/zchunk-1.1.5/src/lib/meson.build src/zchunk-1.1.5/src/lib/meson.build
--- origsrc/zchunk-1.1.5/src/lib/meson.build	2020-01-19 06:32:09.000000000 +0900
+++ src/zchunk-1.1.5/src/lib/meson.build	2020-01-21 09:25:43.149977700 +0900
@@ -6,10 +6,11 @@ subdir('hash')
 subdir('index')
 subdir('dl')
 lib_sources += files('zck.c', 'header.c', 'io.c', 'log.c', 'compint.c', 'error.c')
+argp_dep = cc.find_library('argp')
 zcklib = library('zck',
                         lib_sources,
                         include_directories: inc,
-                        dependencies: [zstd_dep, openssl_dep],
+                        dependencies: [zstd_dep, openssl_dep, argp_dep],
                         install: true,
                         version: meson.project_version(),
                         soversion: so_version)
diff -buprN -x CYGWIN-PATCHES -x aclocal.m4~ -x aclocal.m4t -x autom4te.cache -x config.cache -x config.guess -x config.log -x config.status -x config.sub -x '*SlackBuild*' -x '*.egg-info' -x '*.dll' -x '*.exe' -x '*.o' -x '*.gch' -x '*.gem' -x .gitignore -x '*.jpg' -x '*.png' -x '*.qm' -x '*.svgz' -x '*.tif' -x '*.class' -x '*.jlc' -x '*.pyc' -x '*.pyo' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*.whl' -x '*~' -x '*.stackdump' -x x86_64-pc-cygwin -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x mate-doc-utils.make -x mate-doc-utils.m4 origsrc/zchunk-1.1.5/src/meson.build src/zchunk-1.1.5/src/meson.build
--- origsrc/zchunk-1.1.5/src/meson.build	2020-01-19 06:32:09.000000000 +0900
+++ src/zchunk-1.1.5/src/meson.build	2020-01-21 09:25:43.165605800 +0900
@@ -3,6 +3,7 @@ zck = executable(
     'zck',
     ['zck.c', 'util_common.c'],
     include_directories: inc,
+    dependencies: argp_dep,
     link_with: zcklib,
     install: true
 )
@@ -10,6 +11,7 @@ unzck = executable(
     'unzck',
     ['unzck.c', 'util_common.c'],
     include_directories: inc,
+    dependencies: argp_dep,
     link_with: zcklib,
     install: true
 )
@@ -17,7 +19,7 @@ zckdl = executable(
     'zckdl',
     ['zck_dl.c', 'util_common.c'],
     include_directories: inc,
-    dependencies: curl_dep,
+    dependencies: [curl_dep, argp_dep],
     link_with: zcklib,
     install: true
 )
@@ -25,6 +27,7 @@ zck_gen_zdict = executable(
     'zck_gen_zdict',
     ['zck_gen_zdict.c', 'util_common.c'],
     include_directories: inc,
+    dependencies: argp_dep,
     link_with: zcklib,
     install: true
 )
@@ -32,6 +35,7 @@ zck_read_header = executable(
     'zck_read_header',
     ['zck_read_header.c', 'util_common.c'],
     include_directories: inc,
+    dependencies: argp_dep,
     link_with: zcklib,
     install: true
 )
@@ -39,6 +43,7 @@ zck_delta_size = executable(
     'zck_delta_size',
     ['zck_delta_size.c', 'util_common.c', 'memmem.c'],
     include_directories: inc,
+    dependencies: argp_dep,
     link_with: zcklib,
     install: true
 )
diff -buprN -x CYGWIN-PATCHES -x aclocal.m4~ -x aclocal.m4t -x autom4te.cache -x config.cache -x config.guess -x config.log -x config.status -x config.sub -x '*SlackBuild*' -x '*.egg-info' -x '*.dll' -x '*.exe' -x '*.o' -x '*.gch' -x '*.gem' -x .gitignore -x '*.jpg' -x '*.png' -x '*.qm' -x '*.svgz' -x '*.tif' -x '*.class' -x '*.jlc' -x '*.pyc' -x '*.pyo' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*.whl' -x '*~' -x '*.stackdump' -x x86_64-pc-cygwin -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x mate-doc-utils.make -x mate-doc-utils.m4 origsrc/zchunk-1.1.5/src/unzck.c src/zchunk-1.1.5/src/unzck.c
--- origsrc/zchunk-1.1.5/src/unzck.c	2020-01-19 06:32:09.000000000 +0900
+++ src/zchunk-1.1.5/src/unzck.c	2020-01-21 09:25:43.165605800 +0900
@@ -54,6 +54,10 @@ static struct argp_option options[] = {
     { 0 }
 };
 
+#if defined(stdout)
+#undef stdout
+#endif
+
 struct arguments {
   char *args[1];
   zck_log_type log_level;
diff -buprN -x CYGWIN-PATCHES -x aclocal.m4~ -x aclocal.m4t -x autom4te.cache -x config.cache -x config.guess -x config.log -x config.status -x config.sub -x '*SlackBuild*' -x '*.egg-info' -x '*.dll' -x '*.exe' -x '*.o' -x '*.gch' -x '*.gem' -x .gitignore -x '*.jpg' -x '*.png' -x '*.qm' -x '*.svgz' -x '*.tif' -x '*.class' -x '*.jlc' -x '*.pyc' -x '*.pyo' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*.whl' -x '*~' -x '*.stackdump' -x x86_64-pc-cygwin -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x mate-doc-utils.make -x mate-doc-utils.m4 origsrc/zchunk-1.1.5/src/zck_gen_zdict.c src/zchunk-1.1.5/src/zck_gen_zdict.c
--- origsrc/zchunk-1.1.5/src/zck_gen_zdict.c	2020-01-19 06:32:09.000000000 +0900
+++ src/zchunk-1.1.5/src/zck_gen_zdict.c	2020-01-21 09:25:43.181230800 +0900
@@ -57,6 +57,10 @@ static struct argp_option options[] = {
     { 0 }
 };
 
+#if defined(stdout)
+#undef stdout
+#endif
+
 struct arguments {
   char *args[1];
   char *dir;
