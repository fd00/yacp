--- origsrc/zssh-1.5c/Makefile.in	2025-12-05 08:12:48.543645000 +0900
+++ src/zssh-1.5c/Makefile.in	2025-12-05 08:13:20.327623000 +0900
@@ -35,12 +35,13 @@ EXTRA_DEPS	=  @EXTRA_DEPS@
 # is defined
 #CFLAGS += -DDUMB_KEYBOARD
 
+VPATH = @srcdir@
 
 #End of system specific vars
 
 
 NAME		= zssh
-VERSION		= $(shell cat VERSION)
+VERSION		= $(shell cat @srcdir@/VERSION)
 PRGS		= zssh ztelnet
 MANS		= $(PRGS:=.1)
 # Files to be installed
@@ -110,7 +111,7 @@ $(RPMSPEC) : $(RPMSPEC:.spec=.spec.tpl)
 
 
 version.h : force
-	echo '#define ZSSH_VERSION "' `cat VERSION` '"' > version.h
+	echo '#define ZSSH_VERSION "' ${VERSION} '"' > version.h
 
 fun.h : $(SRC)
 	protos -o fun.h $(SRC)
--- origsrc/zssh-1.5c/configure.in	2025-12-05 08:12:47.082989400 +0900
+++ src/zssh-1.5c/configure.in	2025-12-05 08:15:09.815756100 +0900
@@ -136,5 +136,5 @@ AC_SUBST(EXTRA_CFLAGS)
 AC_SUBST(EXTRA_LIBS)
 AC_SUBST(TERMCAP_LIB)
 
-AC_OUTPUT(Makefile fake_readline/Makefile test/Makefile)
+AC_OUTPUT(Makefile test/Makefile)
 
